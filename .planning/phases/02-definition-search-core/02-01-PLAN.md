---
phase: 02-definition-search-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/queries/queryCatalog.ts
  - tree-sitter-mcp/src/queries/definitionQueryCatalog.ts
  - tree-sitter-mcp/src/definitions/definitionPipeline.ts
  - tree-sitter-mcp/src/definitions/searchDefinitions.ts
  - tree-sitter-mcp/src/definitions/resolveDefinition.ts
  - tree-sitter-mcp/test/definitionPipeline.test.ts
  - tree-sitter-mcp/test/definitionLookup.test.ts
autonomous: true
requirements: [SEM-01, SEM-03, RES-01]
user_setup: []
must_haves:
    truths:
      - "Definition extraction is implemented as an on-demand service that walks discovered workspace files, parses only needed files, and never creates a persistent index or background cache."
      - "Language-specific Tree-sitter queries capture definition nodes, symbol names, and exact source spans for the builtin JavaScript, TypeScript, TSX, and Python grammars."
      - "The backend can both search definitions by name across the active workspace and resolve the best definition target for a discovered symbol descriptor or exact lookup request."
      - "Parse failures and unsupported-language states continue to surface as structured diagnostics instead of silent skips."
    artifacts:
      - path: tree-sitter-mcp/src/queries/definitionQueryCatalog.ts
        provides: Language-specific definition query definitions and query compilation reuse
        contains: symbol.definition
      - path: tree-sitter-mcp/src/definitions/definitionPipeline.ts
        provides: Shared on-demand definition extraction pipeline
        exports:
          - collectFileDefinitions
      - path: tree-sitter-mcp/src/definitions/searchDefinitions.ts
        provides: Workspace definition search service
        exports:
          - searchDefinitions
      - path: tree-sitter-mcp/src/definitions/resolveDefinition.ts
        provides: Direct definition resolution service
        exports:
          - resolveDefinition
      - path: tree-sitter-mcp/test/definitionPipeline.test.ts
        provides: Multi-language definition extraction coverage
        contains: searchDefinitions
      - path: tree-sitter-mcp/test/definitionLookup.test.ts
        provides: Resolution coverage for discovered symbol inputs
        contains: resolveDefinition
    key_links:
      - from: tree-sitter-mcp/src/definitions/definitionPipeline.ts
        to: tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts
        via: definition extraction reuses the Phase 1 on-demand parser wrapper
        pattern: parseWithDiagnostics
      - from: tree-sitter-mcp/src/definitions/definitionPipeline.ts
        to: tree-sitter-mcp/src/queries/definitionQueryCatalog.ts
        via: parsed trees are queried through the language definition capture catalog
        pattern: symbol.definition
      - from: tree-sitter-mcp/src/definitions/searchDefinitions.ts
        to: tree-sitter-mcp/src/definitions/definitionPipeline.ts
        via: workspace search is built on top of shared file-level definition extraction
        pattern: collectFileDefinitions
---

<objective>
Create reusable on-demand definition extraction services that build on Phase 1 workspace snapshots, Tree-sitter parsing, and query infrastructure to search and resolve definitions without introducing a persistent index.

Purpose: Move from generic symbol listing to a dedicated definition-oriented engine that later MCP tools can expose directly.
Output: A language-specific definition query catalog, shared extraction/search/resolve services, and tests proving reliable on-demand definition retrieval across builtin languages.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-01-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-02-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-03-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add language-specific definition query catalog</name>
  <files>tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/queries/definitionQueryCatalog.ts</files>
  <action>Extend the current query layer with definition-oriented captures for builtin languages. Keep query compilation cached, distinguish the full definition node from the symbol-name selection node, and expose query-type metadata for definition search and definition resolution without removing the existing Phase 1 symbol-query path.</action>
  <verify>Each builtin language can return raw definition captures with definition node, name node, symbol kind, and source offsets from a parsed tree.</verify>
  <done>Definition extraction rules live in a reusable catalog instead of being embedded in tool handlers.</done>
</task>

<task type="auto">
  <name>Task 2: Build on-demand definition extraction, workspace search, and resolution services</name>
  <files>tree-sitter-mcp/src/definitions/definitionPipeline.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts</files>
  <action>Implement services that iterate `workspace.searchableFiles`, reuse `parseWithDiagnostics`, extract definitions from supported files, search by name across the workspace, and resolve a best definition target from either a discovered symbol descriptor or an exact lookup request. Respect workspace exclusions, preserve read-only behavior, and keep all work in process memory only.</action>
  <verify>The services can search definitions across the active workspace and resolve an exact definition target without creating a persistent index.</verify>
  <done>Phase 2 has a reusable backend for definition retrieval that later tools can call directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add multi-language pipeline and lookup tests</name>
  <files>tree-sitter-mcp/test/definitionPipeline.test.ts, tree-sitter-mcp/test/definitionLookup.test.ts</files>
  <action>Add temp-workspace tests covering TypeScript, TSX, JavaScript, and Python definitions, parse-failure propagation, and resolution from Phase 1 style symbol payloads. Prove the services only parse files on demand and never depend on prebuilt indexes or background crawling.</action>
  <verify>`npm test` covers multi-language definition extraction, definition lookup, structured diagnostics, and no-index behavior.</verify>
  <done>There is automated proof that the Phase 2 backend can extract and resolve definitions reliably.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes definition pipeline and definition lookup suites
- [ ] Workspace definition search parses files on demand from `workspace.searchableFiles` and does not create a persistent index
- [ ] Builtin languages return definition matches with exact source ranges and symbol kinds
- [ ] Definition resolution returns a best match or a structured diagnostic when the target cannot be resolved
</verification>

<success_criteria>
- All tasks completed
- Definition retrieval backend exists and builds directly on Phase 1 workspace discovery plus parse diagnostics
- A caller can search definitions by name across the workspace and resolve a definition target without tool-specific logic
- Each extracted match carries exact source ranges that later tools can surface directly
- The Phase 2 foundation remains local, read-only, stdio-first, and on-demand
</success_criteria>

<output>
After completion, create `.planning/phases/02-definition-search-core/02-01-SUMMARY.md`
</output>
