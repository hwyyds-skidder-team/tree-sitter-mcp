---
phase: 02-definition-search-core
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - tree-sitter-mcp/src/definitions/definitionTypes.ts
  - tree-sitter-mcp/src/definitions/normalizeDefinitionMatch.ts
  - tree-sitter-mcp/src/definitions/definitionFilters.ts
  - tree-sitter-mcp/src/definitions/searchDefinitions.ts
  - tree-sitter-mcp/src/definitions/resolveDefinition.ts
  - tree-sitter-mcp/src/workspace/resolveWorkspace.ts
  - tree-sitter-mcp/test/definitionNormalization.test.ts
  - tree-sitter-mcp/test/definitionFilters.test.ts
autonomous: true
requirements: [SEM-04, RES-01]
user_setup: []
must_haves:
    truths:
      - "All definition results conform to one shared schema with stable line, column, and offset values for both the full definition range and the symbol-name selection range."
      - "Language, pathPrefix, and symbolKinds filters are normalized and validated once, then reused by both search and resolution services."
      - "Path filters stay rooted inside the configured workspace and normalize Windows and POSIX separators consistently."
      - "Cross-language definition metadata carries consistent kind, container, file path, relativePath, and languageId fields even when underlying Tree-sitter captures differ."
    artifacts:
      - path: tree-sitter-mcp/src/definitions/definitionTypes.ts
        provides: Shared schemas and types for definition payloads and filters
        exports:
          - DefinitionMatchSchema
          - DefinitionFilterSchema
      - path: tree-sitter-mcp/src/definitions/normalizeDefinitionMatch.ts
        provides: Cross-language metadata and range normalization
        contains: selectionRange
      - path: tree-sitter-mcp/src/definitions/definitionFilters.ts
        provides: Reusable filter normalization and validation helpers
        exports:
          - normalizeDefinitionFilters
      - path: tree-sitter-mcp/test/definitionNormalization.test.ts
        provides: Stable metadata and range regression coverage
        contains: selectionRange
      - path: tree-sitter-mcp/test/definitionFilters.test.ts
        provides: Filter normalization and path guard coverage
        contains: pathPrefix
    key_links:
      - from: tree-sitter-mcp/src/definitions/normalizeDefinitionMatch.ts
        to: tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
        via: normalized definition payloads reuse the shared SourceRange shape
        pattern: SourceRangeSchema
      - from: tree-sitter-mcp/src/definitions/definitionFilters.ts
        to: tree-sitter-mcp/src/workspace/resolveWorkspace.ts
        via: path filters normalize and validate inside the active workspace root
        pattern: resolveWorkspacePath
      - from: tree-sitter-mcp/src/definitions/searchDefinitions.ts
        to: tree-sitter-mcp/src/definitions/definitionFilters.ts
        via: search services apply one shared filter normalization layer
        pattern: normalizeDefinitionFilters
---

<objective>
Normalize Phase 2 definition payloads and filters so every language emits consistent metadata and stable locations, and every caller applies the same narrowing semantics.

Purpose: Prevent per-language drift and tool-specific filter forks before the definition workflow is exposed over MCP.
Output: Shared definition schemas, metadata/range normalizers, reusable filter handling, and regression tests for ranges and narrowing behavior.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-03-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-VERIFICATION.md
@.planning/phases/02-definition-search-core/02-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Extract shared definition schemas and metadata normalizers</name>
  <files>tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/definitions/normalizeDefinitionMatch.ts</files>
  <action>Move Phase 2 output shapes out of ad hoc pipeline code into reusable schemas and types. Standardize fields such as `name`, `kind`, `languageId`, `filePath`, `relativePath`, `range`, `selectionRange`, and `containerName`, and normalize cross-language differences so downstream callers always receive one consistent result shape.</action>
  <verify>All definition matches are serialized through common schemas with stable 1-based line/column and byte-offset boundaries.</verify>
  <done>Cross-language definition payloads are normalized before any MCP tool renders them.</done>
</task>

<task type="auto">
  <name>Task 2: Implement reusable filter normalization and apply it to services</name>
  <files>tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts</files>
  <action>Add helpers that validate and normalize language filters, workspace-root-relative path prefixes, symbol-kind arrays, and other narrowing inputs needed by definition search and lookup. Apply the same helper layer to both services so out-of-scope paths fail early, symbol-kind filtering is deterministic, and path separator differences do not change results.</action>
  <verify>The same filter input produces the same normalized behavior across search and resolution services, and out-of-scope path filters are rejected before scanning files.</verify>
  <done>Filter semantics are centralized, deterministic, and workspace-aware.</done>
</task>

<task type="auto">
  <name>Task 3: Add normalization and filter regression tests</name>
  <files>tree-sitter-mcp/test/definitionNormalization.test.ts, tree-sitter-mcp/test/definitionFilters.test.ts</files>
  <action>Add regression tests for cross-language kind/container normalization, stable range boundaries, Windows-style and POSIX-style pathPrefix normalization, out-of-scope path rejection, and language/symbol-kind filter deduplication.</action>
  <verify>`npm test` exercises normalized metadata and filter helpers with mixed-language fixtures and workspace-relative path inputs.</verify>
  <done>Phase 2 has regression coverage that prevents filter drift and unstable location payloads.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes definition normalization and filter suites
- [ ] Every definition result includes stable 1-based line/column boundaries plus offsets for `range` and `selectionRange`
- [ ] `pathPrefix` filters normalize mixed path separators and reject out-of-scope paths
- [ ] Language and symbol-kind filters behave consistently across JavaScript, TypeScript, TSX, and Python
</verification>

<success_criteria>
- All tasks completed
- Every definition match carries stable line/column boundaries and consistent metadata across languages
- Search and resolution services can be narrowed by language, path, and symbol kind through one shared filter layer
- Workspace-relative filter handling is deterministic across OS path separators
- Tool-facing payloads are ready for MCP exposure without per-tool normalization forks
</success_criteria>

<output>
After completion, create `.planning/phases/02-definition-search-core/02-02-SUMMARY.md`
</output>
