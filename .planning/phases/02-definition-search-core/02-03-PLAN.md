---
phase: 02-definition-search-core
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
  - tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/definitionTools.e2e.test.ts
autonomous: true
requirements: [SEM-01, SEM-03, SEM-04, RES-01]
user_setup: []
must_haves:
    truths:
      - "The server exposes read-only MCP tools for workspace-wide definition search and direct definition resolution on top of the Phase 2 services."
      - "Structured tool outputs include normalized filters, exact location ranges, diagnostics, and concise text summaries suitable for AI-agent chaining."
      - "Capabilities and health responses advertise definition-search query types while preserving the stdio-first, local, and on-demand design."
      - "End-to-end stdio tests cover definition search, filtered lookup, resolution, diagnostics, and verify that no persistent index or write path is introduced."
    artifacts:
      - path: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
        provides: Workspace definition search MCP tool
        exports:
          - registerSearchDefinitionsTool
      - path: tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
        provides: Direct definition resolution MCP tool
        exports:
          - registerResolveDefinitionTool
      - path: tree-sitter-mcp/src/server/toolRegistry.ts
        provides: MCP tool registration for definition search
        contains: search_definitions
      - path: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
        provides: Updated capability reporting for Phase 2 tools and query types
        contains: search_definitions
      - path: tree-sitter-mcp/test/definitionTools.e2e.test.ts
        provides: stdio end-to-end coverage for the Phase 2 workflow
        contains: resolve_definition
    key_links:
      - from: tree-sitter-mcp/src/server/toolRegistry.ts
        to: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
        via: registry wires the definition search tool into the MCP server
        pattern: search_definitions
      - from: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
        to: tree-sitter-mcp/src/definitions/searchDefinitions.ts
        via: the tool delegates matching and filtering to the shared backend search service
        pattern: searchDefinitions
      - from: tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
        to: tree-sitter-mcp/src/definitions/resolveDefinition.ts
        via: the tool delegates direct lookup and target resolution to the shared backend service
        pattern: resolveDefinition
---

<objective>
Expose the Phase 2 definition workflow as agent-ready, read-only MCP tools with structured payloads and end-to-end proof through the built stdio server.

Purpose: Complete the user-facing Phase 2 experience using the reusable pipeline and normalization layers built earlier in the phase.
Output: Definition-search MCP tools, registry and capability wiring, updated health reporting, and end-to-end stdio coverage for filtered definition workflows.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-03-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-VERIFICATION.md
@.planning/phases/02-definition-search-core/02-01-SUMMARY.md
@.planning/phases/02-definition-search-core/02-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add read-only definition search and resolution tools</name>
  <files>tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/resolveDefinitionTool.ts</files>
  <action>Register `search_definitions` and `resolve_definition` as read-only MCP tools with Zod schemas and structured payloads. `search_definitions` should accept a name query plus optional `language`, `pathPrefix`, `symbolKinds`, and `limit` filters; `resolve_definition` should accept either a discovered symbol descriptor or a direct lookup request and return one best definition match or an actionable diagnostic.</action>
  <verify>An MCP client can call both tools over stdio and receive structured content matching the normalized Phase 2 schemas.</verify>
  <done>The Phase 2 definition workflow is reachable via MCP without exposing internal services directly.</done>
</task>

<task type="auto">
  <name>Task 2: Update capabilities, health, and textual summaries for Phase 2</name>
  <files>tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts</files>
  <action>Surface the new tool names and definition-oriented query types in capability and health responses. Keep summaries concise but explicit that the server remains local, read-only, stdio-first, and on-demand, and make sure capability payloads continue to advertise workspace constraints and supported languages alongside the new definition workflow.</action>
  <verify>`get_capabilities` and `get_health` report definition-search support and active workspace constraints without implying persistent indexing or write behavior.</verify>
  <done>Clients can discover and trust the Phase 2 tool surface before issuing semantic definition queries.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end stdio coverage for definition workflows</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts</files>
  <action>Launch the built server as an MCP subprocess, set a workspace, search definitions with filters, resolve a definition from a prior search or direct lookup, and assert exact range fields plus actionable diagnostics for unsupported or unresolved cases. Confirm the workflow is purely on-demand and does not mutate files or build a persistent index.</action>
  <verify>`npm test` covers the Phase 2 workflow end-to-end from `set_workspace` through definition search, filtered narrowing, and definition resolution.</verify>
  <done>There is executable evidence that Phase 2 works from an MCP client's perspective.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes updated capabilities/health coverage plus the new definition-tool end-to-end suite
- [ ] `search_definitions` returns symbol kind, file path, relativePath, and exact `range`/`selectionRange` fields for every match
- [ ] `resolve_definition` returns one normalized definition result or a structured diagnostic
- [ ] Language, `pathPrefix`, and `symbolKinds` filters narrow results deterministically
- [ ] The server remains read-only, stdio-first, and on-demand with no persistent index
</verification>

<success_criteria>
- All tasks completed
- A user can search symbol definitions by name across the workspace and receive symbol kind, file path, and exact source range
- A user can resolve the definition target for a discovered symbol or direct lookup request
- A user can narrow definition search by path, language, and symbol kind to reduce noise
- Every returned semantic match includes stable line/column boundaries that another tool can jump to directly
- MCP responses expose concise text plus structured payloads while preserving the Phase 1 local read-only design
</success_criteria>

<output>
After completion, create `.planning/phases/02-definition-search-core/02-03-SUMMARY.md`
</output>
