---
phase: 01-server-foundation-and-workspace-discovery
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - tree-sitter-mcp/src/workspace/workspaceState.ts
  - tree-sitter-mcp/src/workspace/resolveWorkspace.ts
  - tree-sitter-mcp/src/workspace/discoverFiles.ts
  - tree-sitter-mcp/src/workspace/exclusionPolicy.ts
  - tree-sitter-mcp/src/languages/languageRegistry.ts
  - tree-sitter-mcp/src/languages/registerBuiltinGrammars.ts
  - tree-sitter-mcp/test/workspaceDiscovery.test.ts
  - tree-sitter-mcp/test/languageRegistry.test.ts
autonomous: true
requirements: [WORK-01, WORK-02, WORK-03]
user_setup: []
must_haves:
    truths:
      - "Workspace resolution normalizes a target root, stores it in server state, and rejects analysis paths outside that root."
      - "Discovery applies deterministic default exclusions for dependency, generated, build, and vendored paths, plus configurable additional exclusions."
      - "Only files backed by registered Tree-sitter grammars are admitted to downstream semantic flows; unsupported files are classified instead of silently skipped."
      - "The server can enumerate the languages and grammars it currently supports."
    artifacts:
      - path: tree-sitter-mcp/src/workspace/workspaceState.ts
        provides: Workspace root and exclusion state
        min_lines: 20
      - path: tree-sitter-mcp/src/workspace/discoverFiles.ts
        provides: Searchable file discovery pipeline
        min_lines: 30
      - path: tree-sitter-mcp/src/languages/languageRegistry.ts
        provides: Supported language registry
        exports:
          - createLanguageRegistry
      - path: tree-sitter-mcp/test/workspaceDiscovery.test.ts
        provides: Workspace filtering coverage
        contains: excluded
    key_links:
      - from: tree-sitter-mcp/src/workspace/discoverFiles.ts
        to: tree-sitter-mcp/src/workspace/exclusionPolicy.ts
        via: discovery filters paths through exclusion policy
        pattern: shouldExclude
      - from: tree-sitter-mcp/src/languages/registerBuiltinGrammars.ts
        to: tree-sitter-mcp/src/languages/languageRegistry.ts
        via: builtin grammars register into shared registry
        pattern: register
---

<objective>
Implement workspace targeting and discovery services that understand exclusions, supported source files, and Tree-sitter grammar availability so later tools can search the intended codebase safely.

Purpose: Make workspace-aware semantic operations safe, scoped, and explicit before any user-facing search tools are exposed.
Output: Workspace state and resolution services, deterministic exclusion handling, searchable-file discovery, language registry, and focused tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-01-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement workspace root state and exclusion policy</name>
  <files>tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts, tree-sitter-mcp/src/workspace/exclusionPolicy.ts</files>
  <action>Create a workspace state model that records the active root, normalized path handling, and active exclusion rules. Add boundary checks so analysis cannot escape the configured root. Seed the exclusion policy with sensible defaults for dependency, generated, vendored, and build-heavy paths while allowing extra patterns to be layered in later.</action>
  <verify>Workspace resolution rejects invalid roots and out-of-scope paths while preserving a normalized root in server state.</verify>
  <done>The server has a deterministic workspace root model and exclusion policy ready for discovery and tool use.</done>
</task>

<task type="auto">
  <name>Task 2: Build searchable file discovery and language registry</name>
  <files>tree-sitter-mcp/src/workspace/discoverFiles.ts, tree-sitter-mcp/src/languages/languageRegistry.ts, tree-sitter-mcp/src/languages/registerBuiltinGrammars.ts</files>
  <action>Implement a discovery pipeline that scans the configured workspace, filters excluded paths, classifies unsupported files, and returns only files backed by registered Tree-sitter grammars. Add a language registry and builtin grammar registration layer with clear extension-to-language mapping.</action>
  <verify>Discovery returns supported files only, records unsupported inputs, and can enumerate currently registered languages/grammars.</verify>
  <done>Workspace discovery and grammar registration are explicit services rather than ad hoc logic spread across tools.</done>
</task>

<task type="auto">
  <name>Task 3: Add workspace and language tests</name>
  <files>tree-sitter-mcp/test/workspaceDiscovery.test.ts, tree-sitter-mcp/test/languageRegistry.test.ts</files>
  <action>Add fixture-driven tests that prove default exclusions work, supported files are retained, unsupported files are classified, invalid paths are rejected, and the language registry reports builtin support deterministically.</action>
  <verify>`npm test` exercises workspace filtering, supported-file selection, unsupported-file classification, and registry enumeration.</verify>
  <done>There is automated coverage for the workspace and grammar infrastructure that later user-facing tools depend on.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes workspace discovery and language registry suites
- [ ] Excluded dependency/build/generated paths are omitted from searchable file results by default
- [ ] Unsupported files are reported explicitly instead of silently disappearing
- [ ] Workspace root validation prevents out-of-scope analysis paths
</verification>

<success_criteria>
- All tasks completed
- The server can target a workspace root for analysis and limit later operations to supported files inside it
- Excluded generated, vendored, and dependency-heavy paths are handled deterministically
- Supported languages and skipped unsupported files are represented explicitly in server state
</success_criteria>

<output>
After completion, create `.planning/phases/01-server-foundation-and-workspace-discovery/01-02-SUMMARY.md`
</output>
