---
phase: 01-server-foundation-and-workspace-discovery
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/tools/setWorkspaceTool.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/src/tools/listFileSymbolsTool.ts
  - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
  - tree-sitter-mcp/src/queries/queryCatalog.ts
  - tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts
  - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/semanticTools.e2e.test.ts
autonomous: true
requirements: [MCP-02, WORK-01, WORK-02, WORK-03, WORK-04, RES-03]
user_setup: []
must_haves:
    truths:
      - "The server exposes read-only MCP tools for setting or inspecting workspace context and for retrieving capability and health information."
      - "Capability and health responses include supported languages, supported query types, parser mode, active workspace root, and current exclusion constraints."
      - "At least one Tree-sitter-backed semantic query flow is callable from an MCP client over stdio without any persistent index."
      - "Unsupported files, unsupported languages, missing workspace state, and parse failures return structured actionable diagnostics instead of silent skips."
      - "Semantic query execution respects workspace boundaries and exclusion rules."
    artifacts:
      - path: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
        provides: Capability reporting tool
        min_lines: 20
      - path: tree-sitter-mcp/src/tools/getHealthTool.ts
        provides: Health reporting tool
        min_lines: 20
      - path: tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
        provides: Minimal workspace symbol search tool
        min_lines: 30
      - path: tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
        provides: Structured diagnostics
        exports:
          - createDiagnostic
      - path: tree-sitter-mcp/test/semanticTools.e2e.test.ts
        provides: stdio end-to-end MCP coverage
        contains: get_capabilities
    key_links:
      - from: tree-sitter-mcp/src/server/toolRegistry.ts
        to: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
        via: registry wires capabilities tool into MCP server
        pattern: capabilities
      - from: tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
        to: tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts
        via: semantic query uses on-demand parse wrapper
        pattern: parseWithDiagnostics
      - from: tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts
        to: tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
        via: parse failures map to structured diagnostics
        pattern: createDiagnostic
---

<objective>
Expose the user-facing MCP tool surface for workspace control, server introspection, and initial on-demand semantic queries, with actionable diagnostics instead of silent failure paths.

Purpose: Complete the externally visible Phase 1 experience so agents can connect, inspect capabilities, target a workspace, and run minimal semantic queries safely.
Output: Read-only MCP tools for workspace/capabilities/health, initial Tree-sitter-backed semantic query tools, shared diagnostics, and end-to-end stdio tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-01-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-02-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add capability, health, and workspace control tools</name>
  <files>tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts</files>
  <action>Register explicit read-only MCP tools that let a client set or inspect workspace context and retrieve server capability and health information. Responses must include supported languages, supported query types, parser mode, active workspace root, and current exclusion constraints so the server is debuggable before richer search flows exist.</action>
  <verify>An MCP client can call workspace, capability, and health tools over stdio and receive structured payloads with the expected server-state fields.</verify>
  <done>The server has explicit introspection and workspace-control tools instead of relying on logs or hidden state.</done>
</task>

<task type="auto">
  <name>Task 2: Implement minimal semantic query tools with shared diagnostics</name>
  <files>tree-sitter-mcp/src/tools/listFileSymbolsTool.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts, tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts</files>
  <action>Implement a minimal but real on-demand Tree-sitter query surface such as file-symbol listing and workspace-symbol search. Route unsupported files, unsupported languages, missing workspace state, and parse failures through a shared structured diagnostic factory so no invalid input is silently skipped.</action>
  <verify>Semantic query tools can run without a persistent index and return either structured results or structured actionable diagnostics.</verify>
  <done>Phase 1 delivers a usable read-only semantic query flow backed by on-demand Tree-sitter parsing.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end stdio coverage for Phase 1 behavior</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts</files>
  <action>Add end-to-end tests that launch the built server as an MCP subprocess, set a workspace, inspect capability and health responses, verify exclusions are honored, and confirm unsupported-language or parse-failure cases return actionable diagnostics. Cover the on-demand semantics contract so the tests prove no persistent index is required.</action>
  <verify>`npm test` includes stdio end-to-end coverage for workspace setup, introspection, exclusions, semantic tool calls, and diagnostic error paths.</verify>
  <done>There is executable evidence that Phase 1 works from an MCP client's perspective.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes capability, health, and end-to-end semantic tool suites
- [ ] `get_capabilities` and `get_health` return supported languages, query types, parser mode, workspace root, and exclusion constraints
- [ ] Semantic query tools operate only on supported files within the configured workspace and respect exclusions
- [ ] Unsupported-language and parse-failure cases return structured diagnostics with a reason and next step
- [ ] Semantic queries run on demand and do not require or create a persistent index
</verification>

<success_criteria>
- All tasks completed
- A user can inspect server capabilities or health before searching
- A user can point the server at a workspace and have semantic queries limited to supported source files inside that workspace
- Unsupported files or parse failures produce actionable diagnostics rather than silent skips
- An MCP client can call read-only semantic code search tools over local stdio without a persistent index
</success_criteria>

<output>
After completion, create `.planning/phases/01-server-foundation-and-workspace-discovery/01-03-SUMMARY.md`
</output>
