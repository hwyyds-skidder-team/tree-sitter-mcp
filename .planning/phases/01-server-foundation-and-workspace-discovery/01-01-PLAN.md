---
phase: 01-server-foundation-and-workspace-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/package.json
  - tree-sitter-mcp/tsconfig.json
  - tree-sitter-mcp/src/index.ts
  - tree-sitter-mcp/src/server/createServer.ts
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/server/serverContext.ts
  - tree-sitter-mcp/src/config/runtimeConfig.ts
  - tree-sitter-mcp/test/serverBootstrap.test.ts
  - tree-sitter-mcp/README.md
autonomous: true
requirements: [MCP-01]
user_setup: []
must_haves:
    truths:
      - "A standalone npm package exists under tree-sitter-mcp with build, test, and local launch scripts."
      - "The built server can start from tree-sitter-mcp over stdio and complete MCP initialize plus list-tools without eager workspace crawling."
      - "MCP SDK wiring is isolated behind a server factory and tool registry boundary so later workspace and search logic is not embedded in transport bootstrap code."
    artifacts:
      - path: tree-sitter-mcp/package.json
        provides: Standalone package metadata and scripts
        contains: '"name": "tree-sitter-mcp"'
      - path: tree-sitter-mcp/src/index.ts
        provides: stdio entrypoint
        contains: StdioServerTransport
      - path: tree-sitter-mcp/src/server/createServer.ts
        provides: MCP server factory
        exports:
          - createServer
      - path: tree-sitter-mcp/test/serverBootstrap.test.ts
        provides: Bootstrap smoke coverage
        contains: initialize
    key_links:
      - from: tree-sitter-mcp/src/index.ts
        to: tree-sitter-mcp/src/server/createServer.ts
        via: entrypoint uses server factory
        pattern: createServer
      - from: tree-sitter-mcp/src/server/createServer.ts
        to: tree-sitter-mcp/src/server/toolRegistry.ts
        via: server factory registers tools through registry boundary
        pattern: register
---

<objective>
Create the standalone TypeScript MCP server package in `tree-sitter-mcp/` with a stdio-first bootstrap, a clean SDK adapter boundary, and smoke-testable local launch behavior.

Purpose: Establish a runnable server shell that Phase 1 can build on without mixing protocol bootstrap and domain logic.
Output: A buildable TypeScript package, stdio MCP bootstrap, server context abstraction, and smoke tests proving local launch works.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Scaffold the standalone TypeScript package</name>
  <files>tree-sitter-mcp/package.json, tree-sitter-mcp/tsconfig.json, tree-sitter-mcp/README.md</files>
  <action>Create `tree-sitter-mcp/` as an independently runnable npm package. Add package metadata, Node/TypeScript build scripts, a test script, and a README quickstart that shows how to build and run the server from its own directory. Keep dependency choices aligned with a stdio-first MCP server.</action>
  <verify>From `tree-sitter-mcp/`, package scripts exist and `npm run build` is wired to compile the project.</verify>
  <done>The package can be installed and built from `tree-sitter-mcp/` without relying on repo-root glue.</done>
</task>

<task type="auto">
  <name>Task 2: Implement stdio bootstrap and server boundary</name>
  <files>tree-sitter-mcp/src/index.ts, tree-sitter-mcp/src/server/createServer.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/config/runtimeConfig.ts</files>
  <action>Implement the MCP stdio entrypoint, runtime config loading, a server context object, and a tool registry boundary. Keep transport bootstrap in `src/index.ts` and domain/tool registration behind `createServer` and registry helpers so later workspace and semantic features can plug in cleanly.</action>
  <verify>The built server starts over stdio and can answer MCP initialize and list-tools requests with no workspace scan at startup.</verify>
  <done>The server shell is runnable and protocol bootstrap is separated from later feature modules.</done>
</task>

<task type="auto">
  <name>Task 3: Add bootstrap smoke tests</name>
  <files>tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/README.md</files>
  <action>Add smoke tests that launch the compiled server as an MCP subprocess, complete initialize, assert list-tools returns a valid response, and verify startup does not trigger eager workspace crawling or persistent index setup. Document the tested local launch path in the README.</action>
  <verify>`npm test` includes a bootstrap smoke test that passes against the built server.</verify>
  <done>There is automated proof that the standalone server package launches and speaks MCP over stdio.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm run build` succeeds
- [ ] `cd tree-sitter-mcp && npm test` covers MCP initialize and list-tools bootstrap behavior
- [ ] Smoke tests confirm startup does not perform filesystem crawling or persistent index creation
</verification>

<success_criteria>
- All tasks completed
- A user can launch `tree-sitter-mcp` from its dedicated directory as a standalone MCP server
- An MCP client can complete initialize and list available tools over local stdio
- The codebase has a stable bootstrap boundary ready for workspace and semantic tooling
</success_criteria>

<output>
After completion, create `.planning/phases/01-server-foundation-and-workspace-discovery/01-01-SUMMARY.md`
</output>
