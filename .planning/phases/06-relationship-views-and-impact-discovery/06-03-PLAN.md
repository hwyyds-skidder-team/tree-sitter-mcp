---
phase: 06-relationship-views-and-impact-discovery
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified:
  - tree-sitter-mcp/test/relationshipTools.e2e.test.ts
  - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/serverBootstrap.test.ts
  - tree-sitter-mcp/test/searchIndexIntegration.test.ts
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]
user_setup: []
must_haves:
  truths:
    - "Phase 6 must be proven through realistic end-to-end MCP workflows, not only through backend unit tests."
    - "Relationship results remain attributable and deterministic when multiple configured workspaces contain overlapping symbol names and file paths."
    - "The new tool/query type is discoverable through bootstrap, capabilities, and health so agents can rely on it without hidden feature flags."
    - "Relationship retrieval must preserve the indexed freshness/degraded diagnostics model, including read-only behavior when changed files fail to parse."
  artifacts:
    - path: tree-sitter-mcp/test/relationshipTools.e2e.test.ts
      provides: Single-workspace stdio regression coverage for direct relationships, pagination, and impact depth
      contains: get_relationship_view
    - path: tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts
      provides: Federated multi-workspace proof for attribution, narrowing, and duplicate-symbol disambiguation
      contains: workspaceRoots
    - path: tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
      provides: Metadata coverage for the relationship query type and tool visibility
      contains: relationship_view
    - path: tree-sitter-mcp/test/serverBootstrap.test.ts
      provides: Bootstrap-level assertion that the compiled server lists the new relationship tool
      contains: get_relationship_view
    - path: tree-sitter-mcp/test/searchIndexIntegration.test.ts
      provides: Freshness/degraded index validation for relationship workflows after file changes
      contains: get_relationship_view
  key_links:
    - from: tree-sitter-mcp/test/relationshipTools.e2e.test.ts
      to: tree-sitter-mcp/src/tools/getRelationshipViewTool.ts
      via: the stdio suite should prove the full user flow from workspace setup or definition resolution into relationship inspection without mutating the repo
      pattern: get_relationship_view
    - from: tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts
      to: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      via: federated fixtures must prove duplicate names and paths stay disambiguated by `workspaceRoot` through direct and impact-style results
      pattern: workspaceRoot
    - from: tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
      to: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
      via: capability metadata should advertise both the semantic query type and the MCP tool name once Phase 6 lands
      pattern: relationship_view
    - from: tree-sitter-mcp/test/searchIndexIntegration.test.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: relationship retrieval should inherit the same refreshed/degraded index guarantees as the other indexed semantic tools
      pattern: freshness
---

<objective>
Validate the full Phase 6 relationship experience on realistic repositories and server metadata surfaces.

Purpose: Prove that relationship views and impact discovery actually work end-to-end over stdio, stay correct across multiple roots, and preserve the existing indexed freshness/diagnostic guarantees.
Output: Dedicated single-root and multi-root E2E suites plus updated bootstrap/capabilities/index integration tests that make Phase 6 safe to execute and ship.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-relationship-views-and-impact-discovery/06-01-PLAN.md
@.planning/phases/06-relationship-views-and-impact-discovery/06-02-PLAN.md
@tree-sitter-mcp/test/definitionTools.e2e.test.ts
@tree-sitter-mcp/test/referenceTools.e2e.test.ts
@tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts
@tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
@tree-sitter-mcp/test/serverBootstrap.test.ts
@tree-sitter-mcp/test/searchIndexIntegration.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add single-workspace E2E coverage for direct relationships and impact expansion</name>
  <files>tree-sitter-mcp/test/relationshipTools.e2e.test.ts</files>
  <read_first>tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/src/tools/getRelationshipViewTool.ts, tree-sitter-mcp/src/tools/resolveDefinitionTool.ts</read_first>
  <action>Create `tree-sitter-mcp/test/relationshipTools.e2e.test.ts` with a realistic stdio fixture that contains callers, callees, plain references, and at least one additional downstream hop. Assert this exact read-only flow: `set_workspace`, resolve or search for a seed symbol, then call `get_relationship_view` and verify direct `incoming_call`, `incoming_reference`, `outgoing_call`, and `outgoing_reference` edges return precise `workspaceRoot`, `relativePath`, `selectionRange`, and evidence snippets. Add pagination assertions (`limit`/`offset`) plus a `maxDepth: 2` assertion that expands one additional neighborhood hop. Mirror the existing definition/reference E2E convention by proving the workspace files are unchanged before and after the tool sequence.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the new relationship E2E suite passes without mutating its temp workspace fixture.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `get_relationship_view`
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `incoming_call`
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `outgoing_reference`
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `maxDepth`
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/relationshipTools.e2e.test.ts contains `selectionRange`
  </acceptance_criteria>
  <done>Phase 6 has a realistic single-workspace proof that relationship inspection works end-to-end and stays read-only.</done>
</task>

<task type="auto">
  <name>Task 2: Prove workspace attribution and disambiguation in federated relationship views</name>
  <files>tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts</files>
  <read_first>tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/src/tools/getRelationshipViewTool.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts</read_first>
  <action>Create `tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts` with two or more temp roots that intentionally reuse symbol names and relative paths. Assert these exact scenarios: (1) `set_workspace({ roots: [...] })` enables one relationship query across all configured roots, (2) duplicate seed symbols can be disambiguated by `workspaceRoot`, (3) every related symbol and evidence record preserves the correct `workspaceRoot`, (4) `workspaceRoots` filtering removes the non-selected root entirely, (5) `relationshipKinds` filtering removes the opposite direction/kind edges, (6) `workspaceBreakdown` totals match returned results, and (7) `maxDepth: 2` never crosses into unrelated duplicate symbols simply because names match in another workspace. Keep the fixtures grounded in real semantic links rather than synthetic JSON-only mocks.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the federated relationship suite passes alongside the existing multi-workspace search coverage.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `relationshipKinds`
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `maxDepth`
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts contains `root:`
  </acceptance_criteria>
  <done>Relationship views stay attributable and deterministic even when multiple repositories share names and paths.</done>
</task>

<task type="auto">
  <name>Task 3: Extend metadata and freshness regression coverage for the new relationship surface</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts</files>
  <read_first>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/tools/getRelationshipViewTool.ts</read_first>
  <action>Extend the metadata suites so the compiled server lists `get_relationship_view`, and capabilities/health advertise `relationship_view` in `supportedQueryTypes` plus the tool name in `toolNames`. Extend `searchIndexIntegration.test.ts` (or the closest existing index-freshness suite) so a relationship query proves it inherits the same refreshed/degraded behavior as the other indexed semantic tools after file edits or parse failures. Assert that degraded relationship responses still remain read-only and actionable through diagnostics rather than silently returning stale edges.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm bootstrap, capabilities/health, index integration, and both new relationship E2E suites all pass.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `relationship_view`
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `get_relationship_view`
    - tree-sitter-mcp/test/serverBootstrap.test.ts contains `get_relationship_view`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `get_relationship_view`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `freshness`
  </acceptance_criteria>
  <done>Phase 6 is discoverable, diagnosable, and covered by the same freshness guarantees as the rest of the indexed search stack.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new relationship E2E, federated, bootstrap, capability, and freshness coverage
- [ ] Relationship views work over stdio without mutating the workspace fixture
- [ ] Multi-workspace relationship results preserve `workspaceRoot` attribution and honor `workspaceRoots` / `relationshipKinds` narrowing
- [ ] `relationship_view` and `get_relationship_view` are visible through capabilities, health, and bootstrap metadata
- [ ] Relationship responses inherit refreshed/degraded diagnostics instead of serving stale or silent data
</verification>

<success_criteria>
- All tasks completed
- Phase 6 is validated end-to-end, not just at the backend helper level
- Agents can discover and trust the relationship tool through normal MCP metadata surfaces
- Relationship inspection remains deterministic, attributable, and read-only across single-root and multi-root repositories
</success_criteria>

<output>
After completion, create `.planning/phases/06-relationship-views-and-impact-discovery/06-03-SUMMARY.md`
</output>
