---
phase: 06-relationship-views-and-impact-discovery
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - tree-sitter-mcp/src/relationships/getRelationshipView.ts
  - tree-sitter-mcp/src/tools/getRelationshipViewTool.ts
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/test/relationshipPipeline.test.ts
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]
user_setup: []
must_haves:
  truths:
    - "Direct incoming and outgoing relationships are derived from indexed semantic evidence plus definition resolution, not from grep-style text adjacency."
    - "Relationship results preserve precise source locations and workspace attribution on both the related symbol and the evidence edge that connected it to the seed."
    - "Filtering by workspace, language, and relationship kind happens before pagination, while `maxDepth` expands only a small impact-oriented neighborhood with stable dedupe."
    - "The user-facing tool stays read-only, reuses freshness/index metadata patterns, and becomes discoverable through bootstrap, capabilities, and health surfaces."
  artifacts:
    - path: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      provides: Relationship traversal, impact-depth expansion, dedupe, and payload shaping
      exports:
        - getRelationshipView
    - path: tree-sitter-mcp/src/tools/getRelationshipViewTool.ts
      provides: Read-only MCP tool exposure for direct relationship and small impact inspection
      contains: get_relationship_view
    - path: tree-sitter-mcp/src/server/toolRegistry.ts
      provides: Server registration for the new relationship-view tool
      contains: registerGetRelationshipViewTool
    - path: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
      provides: Capability metadata that advertises `relationship_view` and `get_relationship_view`
      contains: relationship_view
    - path: tree-sitter-mcp/test/relationshipPipeline.test.ts
      provides: Focused backend coverage for traversal, filters, dedupe, and pagination before E2E validation
      contains: hopCount
  key_links:
    - from: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      to: tree-sitter-mcp/src/definitions/resolveDefinition.ts
      via: the relationship pipeline resolves the seed symbol and any enclosing owner symbols through the same exact definition resolver used by other semantic flows
      pattern: resolveDefinition
    - from: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      to: tree-sitter-mcp/src/references/searchReferences.ts
      via: incoming relationships should be sourced from the indexed reference search path so call/reference classification and freshness handling stay consistent
      pattern: searchReferences
    - from: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: outgoing relationships and impact expansion inspect fresh indexed records rather than reparsing ad hoc per hop
      pattern: getFreshRecords
    - from: tree-sitter-mcp/src/tools/getRelationshipViewTool.ts
      to: tree-sitter-mcp/src/results/workspaceBreakdown.ts
      via: the tool payload should explain searched workspaces and returned edges with the same structured breakdown already used by broad search tools
      pattern: createWorkspaceBreakdown
---

<objective>
Implement the relationship retrieval backend and expose it as a read-only MCP tool.

Purpose: Turn the new schema/filter contract into an actual relationship view that can show direct incoming/outgoing semantic links, paginate/filter them, and optionally expand one extra impact hop without leaving indexed search workflows.
Output: A relationship traversal backend, a `get_relationship_view` MCP tool, capabilities/health/tool-registry wiring, and focused pipeline tests that prove the core behavior before full E2E validation.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-relationship-views-and-impact-discovery/06-01-PLAN.md
@tree-sitter-mcp/src/definitions/resolveDefinition.ts
@tree-sitter-mcp/src/references/searchReferences.ts
@tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
@tree-sitter-mcp/src/server/toolRegistry.ts
@tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
@tree-sitter-mcp/src/tools/getHealthTool.ts
@tree-sitter-mcp/src/results/paginateResults.ts
@tree-sitter-mcp/src/results/workspaceBreakdown.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement relationship traversal for direct links and one extra impact hop</name>
  <files>tree-sitter-mcp/src/relationships/getRelationshipView.ts</files>
  <read_first>tree-sitter-mcp/src/relationships/relationshipTypes.ts, tree-sitter-mcp/src/relationships/relationshipFilters.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/results/paginateResults.ts</read_first>
  <action>Create `tree-sitter-mcp/src/relationships/getRelationshipView.ts` with exact export `getRelationshipView`. The backend must resolve the seed symbol first, normalize relationship filters, and then gather direct edges in two directions. For incoming edges, reuse `searchReferences` so evidence classification (`call` vs `reference`) and freshness stay aligned with the existing indexed reference flow, then resolve each reference's `enclosingContext` back to the owning symbol in the same workspace/file to produce `incoming_call` or `incoming_reference` edges. For outgoing edges, inspect fresh indexed references that fall within the resolved definition range of the frontier symbol, resolve each referenced name to a concrete definition, and emit `outgoing_call` or `outgoing_reference` edges with the original indexed reference as evidence. Apply stable dedupe by `{ relationshipKind, relatedSymbol.workspaceRoot, relatedSymbol.relativePath, relatedSymbol.selectionRange.start.offset, evidence.workspaceRoot, evidence.relativePath, evidence.selectionRange.start.offset, hopCount }`, skip unresolved/self edges with diagnostics, default `maxDepth` to `1`, and allow breadth-first expansion only up to `2` hops for small impact discovery.</action>
  <verify>Read the traversal code and then run `cd tree-sitter-mcp && npm test` after Task 3 adds focused pipeline assertions.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `export async function getRelationshipView`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `searchReferences`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `resolveDefinition`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `getFreshRecords`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `incoming_call`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `outgoing_reference`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `maxDepth`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `hopCount`
  </acceptance_criteria>
  <done>The server can compute relationship edges from real semantic evidence and optionally expand one extra hop for impact inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Expose `get_relationship_view` with workspace-aware payload shaping and metadata</name>
  <files>tree-sitter-mcp/src/tools/getRelationshipViewTool.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts</files>
  <read_first>tree-sitter-mcp/src/relationships/getRelationshipView.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/results/workspaceBreakdown.ts, tree-sitter-mcp/src/results/paginateResults.ts</read_first>
  <action>Create `tree-sitter-mcp/src/tools/getRelationshipViewTool.ts` and register an exact read-only MCP tool named `get_relationship_view`. The tool input must forward the relationship request schema from 06-01. The output must include exact additive fields `workspaceRoot`, `workspaceRoots`, `seed`, `results`, `searchedFiles`, `matchedFiles`, `pagination`, `workspaceBreakdown`, `freshness`, `diagnostic`, and `diagnostics`, where `results` are paginated relationship edges and `workspaceBreakdown` reuses the existing per-workspace summary pattern. Update `toolRegistry.ts` to register the new tool, and update `getCapabilitiesTool.ts` / `getHealthTool.ts` so both `supportedQueryTypes` and `toolNames` advertise `relationship_view` and `get_relationship_view`. Keep `readOnlyHint: true`, make the text response mention multi-workspace scope when more than one root is selected, and preserve the existing freshness-warning text pattern used by indexed search tools.</action>
  <verify>Inspect the new tool schema plus capabilities/health metadata and confirm the tool advertises one read-only relationship surface instead of a second transport or mutation path.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/tools/getRelationshipViewTool.ts contains `get_relationship_view`
    - tree-sitter-mcp/src/tools/getRelationshipViewTool.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/src/tools/getRelationshipViewTool.ts contains `pagination`
    - tree-sitter-mcp/src/tools/getRelationshipViewTool.ts contains `freshness`
    - tree-sitter-mcp/src/server/toolRegistry.ts contains `registerGetRelationshipViewTool`
    - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts contains `relationship_view`
    - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts contains `get_relationship_view`
    - tree-sitter-mcp/src/tools/getHealthTool.ts contains `relationship_view`
  </acceptance_criteria>
  <done>Relationship discovery becomes a first-class MCP workflow with the same structured metadata standards as the existing search tools.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused backend coverage for traversal, filtering, and pagination</name>
  <files>tree-sitter-mcp/test/relationshipPipeline.test.ts</files>
  <read_first>tree-sitter-mcp/test/referencePipeline.test.ts, tree-sitter-mcp/test/referenceSearch.test.ts, tree-sitter-mcp/src/relationships/getRelationshipView.ts, tree-sitter-mcp/src/relationships/relationshipFilters.ts</read_first>
  <action>Create `tree-sitter-mcp/test/relationshipPipeline.test.ts` with in-process fixtures that prove the backend contract before E2E: direct incoming/outgoing edges are produced from indexed evidence, `relationshipKinds` filtering removes the opposite edge kinds, `workspaceRoots` and `language` narrowing trim the result set before pagination, `maxDepth: 2` expands one additional frontier without infinite loops, and duplicate/self/unresolved edges are deduped or skipped with diagnostics instead of leaking malformed relationships. Keep this suite focused on the relationship backend, not stdio transport.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the relationship pipeline suite passes along with existing reference/definition/indexed-search tests.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `incoming_call`
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `outgoing_reference`
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `relationshipKinds`
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `maxDepth`
    - tree-sitter-mcp/test/relationshipPipeline.test.ts contains `hopCount`
  </acceptance_criteria>
  <done>The relationship engine is verified locally before broader client-facing E2E coverage is added.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new relationship backend suite
- [ ] `get_relationship_view` returns direct incoming/outgoing semantic links with precise evidence and workspace attribution
- [ ] Workspace/language/relationship-kind filters narrow results before pagination and before the optional second hop expands further
- [ ] `maxDepth: 2` enables a small impact neighborhood while remaining read-only and deduped
- [ ] Capabilities/health/bootstrap metadata advertise the relationship query type and tool consistently
</verification>

<success_criteria>
- All tasks completed
- Phase 6 ships one read-only relationship tool that works on top of the indexed search foundation
- Users can inspect direct semantic relationships and a small impact neighborhood without leaving MCP search workflows
- Every returned edge explains both who is related and which indexed reference proved the relationship
</success_criteria>

<output>
After completion, create `.planning/phases/06-relationship-views-and-impact-discovery/06-02-SUMMARY.md`
</output>
