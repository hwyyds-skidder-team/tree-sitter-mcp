---
phase: 06-relationship-views-and-impact-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/relationships/relationshipTypes.ts
  - tree-sitter-mcp/src/relationships/relationshipFilters.ts
  - tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts
  - tree-sitter-mcp/test/relationshipFilters.test.ts
autonomous: true
requirements: [REL-01, REL-02, REL-03, REL-04]
user_setup: []
must_haves:
  truths:
    - "Relationship view contracts stay read-only and build on the existing definition/reference/index primitives instead of introducing transport or write behavior."
    - "Every relationship edge carries both the related symbol definition metadata and the concrete reference evidence that justified the link, including workspace attribution and source locations."
    - "Relationship filtering normalizes workspace, language, relationship kind, and impact depth with the same additive multi-workspace conventions already used by definition/reference search."
    - "Phase 6 starts with one first-class semantic query type, `relationship_view`, that can express direct links and small impact neighborhoods through `maxDepth` rather than a second parallel tool."
  artifacts:
    - path: tree-sitter-mcp/src/relationships/relationshipTypes.ts
      provides: Relationship request/result schemas and canonical relationship kind enums
      contains: RelationshipKindSchema
    - path: tree-sitter-mcp/src/relationships/relationshipFilters.ts
      provides: Shared normalization for relationship workspace/language/kind/depth filters
      exports:
        - normalizeRelationshipFilters
    - path: tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts
      provides: Query-type registration for relationship-aware retrieval
      exports:
        - listRelationshipQueryTypes
    - path: tree-sitter-mcp/test/relationshipFilters.test.ts
      provides: Regression coverage for relationship filter defaults, normalization, and validation
      contains: relationshipKinds
  key_links:
    - from: tree-sitter-mcp/src/relationships/relationshipTypes.ts
      to: tree-sitter-mcp/src/definitions/definitionTypes.ts
      via: each relationship edge reuses the definition match contract for the related symbol target instead of inventing a parallel symbol shape
      pattern: DefinitionMatchSchema
    - from: tree-sitter-mcp/src/relationships/relationshipTypes.ts
      to: tree-sitter-mcp/src/references/referenceTypes.ts
      via: every relationship edge must preserve the indexed reference evidence that justified the semantic link
      pattern: ReferenceMatchSchema
    - from: tree-sitter-mcp/src/relationships/relationshipFilters.ts
      to: tree-sitter-mcp/src/definitions/definitionFilters.ts
      via: workspace and language normalization should mirror the existing multi-workspace search conventions so callers do not learn a second filtering model
      pattern: workspaceRoots
    - from: tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts
      to: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
      via: Phase 6 introduces one additive semantic query type that capabilities/health can advertise once the tool is wired
      pattern: relationship_view
---

<objective>
Model the relationship-view contract before traversal code lands.

Purpose: Define one clear schema and filter layer for direct incoming/outgoing links plus small impact neighborhoods, so Phase 6 implementation can reuse existing search/index primitives without guessing request or payload shape later.
Output: A relationship schema module, a normalization module, a relationship query catalog entry, and focused regression tests that lock the contract before tool wiring begins.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-multi-workspace-search-and-result-quality/05-02-PLAN.md
@.planning/phases/05-multi-workspace-search-and-result-quality/05-03-PLAN.md
@tree-sitter-mcp/src/definitions/definitionTypes.ts
@tree-sitter-mcp/src/definitions/definitionFilters.ts
@tree-sitter-mcp/src/references/referenceTypes.ts
@tree-sitter-mcp/src/references/referenceFilters.ts
@tree-sitter-mcp/src/queries/queryCatalog.ts
@tree-sitter-mcp/src/results/paginateResults.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define the canonical relationship schemas and request contract</name>
  <files>tree-sitter-mcp/src/relationships/relationshipTypes.ts</files>
  <read_first>tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/context/contextTypes.ts, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Create `tree-sitter-mcp/src/relationships/relationshipTypes.ts` and define the exact contract for Phase 6. Add `RelationshipKindSchema` with the exact values `incoming_call`, `incoming_reference`, `outgoing_call`, and `outgoing_reference`. Add a `RelationshipEdgeSchema` with the exact required fields `relationshipKind`, `hopCount`, `relatedSymbol`, and `evidence`, where `relatedSymbol` reuses `DefinitionMatchSchema` and `evidence` reuses `ReferenceMatchSchema`. Add a `RelationshipViewRequestSchema` that accepts a seed via exact additive `symbol` or `lookup`, plus exact additive filters `workspaceRoots`, `language`, `relationshipKinds`, `maxDepth`, `limit`, and `offset`. `maxDepth` must be constrained to a small read-only neighborhood (`1..2`) so the tool can support direct links and one extra impact hop without opening whole-program graph scope.</action>
  <verify>Read the schema file and then run `cd tree-sitter-mcp && npm test` after Task 3 adds focused validation coverage.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `RelationshipKindSchema`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `incoming_call`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `incoming_reference`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `outgoing_call`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `outgoing_reference`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `hopCount`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `relatedSymbol: DefinitionMatchSchema`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `evidence: ReferenceMatchSchema`
    - tree-sitter-mcp/src/relationships/relationshipTypes.ts contains `maxDepth: z.number().int().min(1).max(2).optional()`
  </acceptance_criteria>
  <done>Phase 6 has one concrete schema language for relationship edges, pagination inputs, and small impact traversal depth.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize relationship filters with the existing multi-workspace conventions</name>
  <files>tree-sitter-mcp/src/relationships/relationshipFilters.ts</files>
  <read_first>tree-sitter-mcp/src/relationships/relationshipTypes.ts, tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/references/referenceFilters.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/languages/languageRegistry.ts</read_first>
  <action>Create `tree-sitter-mcp/src/relationships/relationshipFilters.ts` with exact exports `normalizeRelationshipFilters`, `matchesRelationshipFilters`, and `DEFAULT_RELATIONSHIP_KINDS`. Normalize workspace scope through the configured root list, lowercase and validate `language` using the language registry, dedupe `relationshipKinds` while preserving canonical enum order, default `maxDepth` to `1`, and surface actionable diagnostics when callers request an unknown workspace root, unsupported language, or out-of-range depth. Keep the filter model intentionally aligned to the roadmap scope: workspace, language, relationship kind, pagination, and impact depth — do not widen Phase 6 to path-prefix or write-oriented controls.</action>
  <verify>Review the normalization helper and then run `cd tree-sitter-mcp && npm test` after Task 3 adds coverage for dedupe/default/error cases.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `export const DEFAULT_RELATIONSHIP_KINDS`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `export function normalizeRelationshipFilters`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `export function matchesRelationshipFilters`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `workspaceRoots`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `relationshipKinds`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `maxDepth`
    - tree-sitter-mcp/src/relationships/relationshipFilters.ts contains `languageRegistry`
  </acceptance_criteria>
  <done>The relationship backend gets one reusable normalization path that behaves like the rest of the multi-workspace search surface.</done>
</task>

<task type="auto">
  <name>Task 3: Register the relationship query type and lock the schema/filter contract with tests</name>
  <files>tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts, tree-sitter-mcp/test/relationshipFilters.test.ts</files>
  <read_first>tree-sitter-mcp/src/queries/definitionQueryCatalog.ts, tree-sitter-mcp/src/queries/referenceQueryCatalog.ts, tree-sitter-mcp/src/relationships/relationshipTypes.ts, tree-sitter-mcp/src/relationships/relationshipFilters.ts</read_first>
  <action>Create `tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts` with exact export `listRelationshipQueryTypes` that returns `relationship_view`. Add `tree-sitter-mcp/test/relationshipFilters.test.ts` to prove the contract before traversal code exists: defaults to all four relationship kinds, `maxDepth` defaults to `1`, duplicate `relationshipKinds` collapse deterministically, unsupported languages/workspaces return diagnostics, and `maxDepth` rejects values outside `1..2`. Keep this suite focused on contract validation, not graph traversal.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the new relationship filter suite passes without requiring any tool wiring yet.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts contains `relationship_view`
    - tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts contains `export function listRelationshipQueryTypes`
    - tree-sitter-mcp/test/relationshipFilters.test.ts contains `relationshipKinds`
    - tree-sitter-mcp/test/relationshipFilters.test.ts contains `maxDepth`
    - tree-sitter-mcp/test/relationshipFilters.test.ts contains `workspaceRoots`
  </acceptance_criteria>
  <done>Phase 6 starts from a locked schema/filter contract instead of letting the traversal tool invent payloads ad hoc.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new relationship schema/filter suite
- [ ] Relationship requests can express direct edges and one extra impact hop through `maxDepth` without widening scope beyond read-only search
- [ ] Every relationship edge schema includes both the related definition target and the indexed reference evidence that justified the link
- [ ] Relationship filter normalization matches existing multi-workspace conventions for workspace and language inputs
</verification>

<success_criteria>
- All tasks completed
- Phase 6 has a stable contract for relationship kinds, filters, pagination, and small impact traversal depth
- Workspace attribution and precise source locations are guaranteed in the schema, not added later as an afterthought
- The upcoming traversal/tool work can build on explicit request/result types instead of inferred payload shapes
</success_criteria>

<output>
After completion, create `.planning/phases/06-relationship-views-and-impact-discovery/06-01-SUMMARY.md`
</output>
