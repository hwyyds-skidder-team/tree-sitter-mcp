---
phase: 04-persistent-indexing-and-query-freshness
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - tree-sitter-mcp/src/tools/setWorkspaceTool.ts
  - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
  - tree-sitter-mcp/src/definitions/searchDefinitions.ts
  - tree-sitter-mcp/src/definitions/resolveDefinition.ts
  - tree-sitter-mcp/src/references/searchReferences.ts
  - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
  - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
  - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
  - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
  - tree-sitter-mcp/test/searchIndexIntegration.test.ts
  - tree-sitter-mcp/test/definitionLookup.test.ts
  - tree-sitter-mcp/test/referenceSearch.test.ts
  - tree-sitter-mcp/test/semanticTools.e2e.test.ts
autonomous: true
requirements: [PERF-01, PERF-02]
user_setup: []
must_haves:
  truths:
    - "`set_workspace` builds or reloads a persistent index before workspace-wide search tools answer their first request for a workspace configuration."
    - "Repeated semantic searches reuse indexed records instead of reparsing every searchable file on every call."
    - "Changed files are refreshed incrementally, deleted files are removed from the active index, and stale records from failed refreshes are excluded from results."
    - "Definition search, definition resolution, reference search, and workspace symbol search preserve their existing filters and ordering while switching to indexed data."
  artifacts:
    - path: tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
      provides: File-level semantic extraction for symbols, definitions, references, and file metadata
      exports:
        - collectIndexedFileSemantics
    - path: tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
      provides: Full workspace indexing for the active searchable file snapshot
      exports:
        - buildWorkspaceIndex
    - path: tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
      provides: Incremental refresh and stale-file pruning for changed workspaces
      exports:
        - refreshWorkspaceIndex
    - path: tree-sitter-mcp/src/tools/setWorkspaceTool.ts
      provides: Workspace bootstrap that waits for persistent-index readiness
      contains: await context.semanticIndex
    - path: tree-sitter-mcp/src/definitions/searchDefinitions.ts
      provides: Definition search over indexed records
      contains: getFreshRecords
    - path: tree-sitter-mcp/src/definitions/resolveDefinition.ts
      provides: Definition resolution over indexed records with existing ranking semantics
      contains: getFreshRecords
    - path: tree-sitter-mcp/src/references/searchReferences.ts
      provides: Reference search over indexed records with stale-record exclusion
      contains: getFreshRecords
    - path: tree-sitter-mcp/test/searchIndexIntegration.test.ts
      provides: Regression coverage for repeated-query reuse and incremental refresh
      contains: refreshed
  key_links:
    - from: tree-sitter-mcp/src/tools/setWorkspaceTool.ts
      to: tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
      via: workspace setup blocks on an initial full build for the active snapshot
      pattern: buildWorkspaceIndex
    - from: tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
      to: tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
      via: incremental refresh reparses only changed files through one extraction path
      pattern: collectIndexedFileSemantics
    - from: tree-sitter-mcp/src/definitions/searchDefinitions.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: definition queries read freshness-checked indexed records instead of reparsing `workspace.searchableFiles`
      pattern: getFreshRecords
    - from: tree-sitter-mcp/src/references/searchReferences.ts
      to: tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
      via: reference queries require a fresh index and drop changed-file records when refresh fails
      pattern: refreshWorkspaceIndex
---

<objective>
Build the persistent index and switch Phase 4 search backends to use it.

Purpose: Make repeated semantic searches materially faster while preserving trustworthy results through incremental refresh and stale-record exclusion.
Output: Full-build and refresh workflows, indexed search integration, and regression tests for repeated queries and file changes.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-01-PLAN.md
@tree-sitter-mcp/src/tools/setWorkspaceTool.ts
@tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
@tree-sitter-mcp/src/definitions/searchDefinitions.ts
@tree-sitter-mcp/src/definitions/resolveDefinition.ts
@tree-sitter-mcp/src/references/searchReferences.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Build indexed semantic records for every searchable file</name>
  <files>tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts</files>
  <read_first>tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts, tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/queries/definitionQueryCatalog.ts, tree-sitter-mcp/src/queries/referenceQueryCatalog.ts, tree-sitter-mcp/src/context/extractEnclosingContext.ts, tree-sitter-mcp/src/context/contextSnippet.ts, tree-sitter-mcp/src/indexing/indexTypes.ts</read_first>
  <action>Create `collectIndexedFileSemantics` that parses one file and returns a record with exact fields `contentHash`, `mtimeMs`, `sizeBytes`, `symbols`, `definitions`, `references`, and `diagnostics`. Reuse `extractSymbols`, `extractDefinitionMatches`, `captureReferenceNodes`, `extractEnclosingContext`, and `createContextSnippet` so indexed reference records already carry `snippet`, `selectionRange`, `enclosingContext`, and `contextSnippet`. Implement `buildWorkspaceIndex` to walk `workspace.searchableFiles`, build records for every file, write the full `manifest.json` and `records.json`, and mark the coordinator state as `fresh` when the full build succeeds.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` after the integration tests in Task 3 are added, then inspect the persisted manifest/records written for a temp workspace.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts contains `contentHash`
    - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts contains `extractDefinitionMatches`
    - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts contains `captureReferenceNodes`
    - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts contains `workspace.searchableFiles`
    - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts contains `records.json`
    - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts contains `markFresh`
  </acceptance_criteria>
  <done>The server can build a complete indexed semantic snapshot for a workspace instead of reparsing on each query.</done>
</task>

<task type="auto">
  <name>Task 2: Add incremental refresh and block search until the index is ready</name>
  <files>tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts</files>
  <read_first>tree-sitter-mcp/src/tools/setWorkspaceTool.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/src/indexing/indexStorage.ts, tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts, tree-sitter-mcp/src/workspace/workspaceState.ts</read_first>
  <action>Implement `refreshWorkspaceIndex` to compare the current workspace snapshot against persisted records using the exact metadata tuple `{ mtimeMs, sizeBytes, contentHash }`, reparse only changed or newly added files, remove records for deleted files, and collect `degradedFiles` when a changed file fails to refresh. Update `semanticIndexCoordinator.ts` so `replaceWorkspace` triggers a full rebuild when the workspace root or exclusions change, and expose `ensureReady()` / `ensureFresh()` methods that search services can await. Update `setWorkspaceTool.ts` to call the coordinator, wait for the initial build or rebuild to finish before returning success, and never fall back to the old on-demand full-workspace parse path while the first build is still in progress.</action>
  <verify>Exercise `set_workspace` in end-to-end tests and prove the first search request only returns after the initial build completes.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts contains `mtimeMs`
    - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts contains `sizeBytes`
    - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts contains `degradedFiles`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `ensureReady`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `ensureFresh`
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `await context.semanticIndex`
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `replaceWorkspace`
  </acceptance_criteria>
  <done>Workspace changes refresh only the necessary files, and no workspace-wide search runs before the current index is ready.</done>
</task>

<task type="auto">
  <name>Task 3: Route semantic searches through freshness-checked indexed records</name>
  <files>tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts, tree-sitter-mcp/test/definitionLookup.test.ts, tree-sitter-mcp/test/referenceSearch.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts</files>
  <read_first>tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, .planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md</read_first>
  <action>Replace the loops that currently iterate `context.workspace.searchableFiles` and reparse files in `search_workspace_symbols`, `searchDefinitions`, `resolveDefinition`, and `searchReferences` with reads from `await context.semanticIndex.getFreshRecords()`. Preserve the existing filter fields (`language`, `pathPrefix`, `symbolKinds`, `limit`, direct lookup ranking, reference pagination) exactly as they work today. When `ensureFresh()` reports a changed file whose refresh failed, exclude that file's stale record from query results and surface the degraded file in returned diagnostics rather than using its previous data. Add regression tests proving repeated queries reuse indexed records, editing one file only refreshes that file, and changed files with broken syntax do not leak stale definitions or references into results.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the search integration, definition lookup, reference search, and semantic E2E suites pass with indexed backends.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `getFreshRecords`
    - tree-sitter-mcp/src/definitions/searchDefinitions.ts contains `getFreshRecords`
    - tree-sitter-mcp/src/definitions/resolveDefinition.ts contains `getFreshRecords`
    - tree-sitter-mcp/src/references/searchReferences.ts contains `getFreshRecords`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `refreshed`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `degraded`
    - tree-sitter-mcp/test/referenceSearch.test.ts contains `degraded`
    - tree-sitter-mcp/test/semanticTools.e2e.test.ts contains `set_workspace`
  </acceptance_criteria>
  <done>Workspace-wide semantic search tools now reuse persistent indexed records and refresh only what changed before returning results.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new indexed search integration suites
- [ ] `set_workspace` waits for an initial index build or rebuild before returning a ready workspace
- [ ] Repeated workspace-wide semantic searches reuse indexed records instead of reparsing every file
- [ ] Changed files are refreshed incrementally and broken changed files do not return stale definitions or references
</verification>

<success_criteria>
- All tasks completed
- Persistent indexing is active for workspace-wide semantic search flows
- Incremental refresh keeps repeated queries fast without silently serving stale results
- Existing search filters, ranking, ordering, and read-only behavior remain intact
- Regression tests prove repeated-query reuse and stale-file exclusion work on real fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistent-indexing-and-query-freshness/04-02-SUMMARY.md`
</output>
