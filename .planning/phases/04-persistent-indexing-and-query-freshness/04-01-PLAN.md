---
phase: 04-persistent-indexing-and-query-freshness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/config/runtimeConfig.ts
  - tree-sitter-mcp/src/server/serverContext.ts
  - tree-sitter-mcp/src/workspace/workspaceState.ts
  - tree-sitter-mcp/src/indexing/indexTypes.ts
  - tree-sitter-mcp/src/indexing/workspaceFingerprint.ts
  - tree-sitter-mcp/src/indexing/indexStorage.ts
  - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
  - tree-sitter-mcp/test/indexStorage.test.ts
  - tree-sitter-mcp/test/semanticIndexCoordinator.test.ts
autonomous: true
requirements: [PERF-01, PERF-02]
user_setup: []
must_haves:
  truths:
    - "Persistent semantic index state is versioned, fingerprinted per workspace, and stored on disk outside the target workspace so it can survive server restarts."
    - "The server tracks index freshness with the exact states `fresh`, `refreshed`, `rebuilding`, and `degraded` before query integration begins."
    - "Workspace summaries and server context can carry persistent-index metadata without changing the read-only MCP boundary."
    - "Schema-version mismatches invalidate old index data instead of silently reusing incompatible records."
  artifacts:
    - path: tree-sitter-mcp/src/config/runtimeConfig.ts
      provides: Runtime configuration for persistent semantic indexing
      contains: TREE_SITTER_MCP_INDEX_DIR
    - path: tree-sitter-mcp/src/indexing/indexTypes.ts
      provides: Zod schemas and types for index manifests, per-file records, and freshness metadata
      exports:
        - FreshnessStateSchema
        - WorkspaceIndexManifestSchema
        - IndexedFileSemanticRecordSchema
        - WorkspaceIndexSummarySchema
    - path: tree-sitter-mcp/src/indexing/workspaceFingerprint.ts
      provides: Stable workspace fingerprint generation from root, exclusions, and schema version
      exports:
        - createWorkspaceFingerprint
    - path: tree-sitter-mcp/src/indexing/indexStorage.ts
      provides: Persistent manifest and record loading/saving under the configured index directory
      exports:
        - loadWorkspaceIndex
        - saveWorkspaceIndex
    - path: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      provides: In-memory coordination for persistent index lifecycle state
      exports:
        - createSemanticIndexCoordinator
    - path: tree-sitter-mcp/src/workspace/workspaceState.ts
      provides: Workspace summary support for index metadata
      contains: index
    - path: tree-sitter-mcp/test/indexStorage.test.ts
      provides: Regression coverage for manifest persistence and schema invalidation
      contains: TREE_SITTER_MCP_INDEX_DIR
    - path: tree-sitter-mcp/test/semanticIndexCoordinator.test.ts
      provides: Regression coverage for coordinator state transitions and summary shaping
      contains: degraded
  key_links:
    - from: tree-sitter-mcp/src/server/serverContext.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: server context owns the persistent-index coordinator alongside workspace state
      pattern: createSemanticIndexCoordinator
    - from: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      to: tree-sitter-mcp/src/indexing/indexStorage.ts
      via: coordinator loads and persists manifest/record snapshots through one storage layer
      pattern: saveWorkspaceIndex
    - from: tree-sitter-mcp/src/workspace/workspaceState.ts
      to: tree-sitter-mcp/src/indexing/indexTypes.ts
      via: workspace summaries expose the same freshness and coverage fields used by the index manifest
      pattern: WorkspaceIndexSummarySchema
---

<objective>
Create the persistent semantic-index foundation for Phase 4.

Purpose: Establish stable disk-backed storage, workspace fingerprinting, and in-memory lifecycle coordination before any search flow switches away from full on-demand reparsing.
Output: New indexing modules, server/workspace state wiring, and unit tests that lock the lifecycle contract.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md
@.planning/phases/03-reference-search-and-agent-ready-results/03-03-SUMMARY.md
@tree-sitter-mcp/src/config/runtimeConfig.ts
@tree-sitter-mcp/src/server/serverContext.ts
@tree-sitter-mcp/src/workspace/workspaceState.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define persistent index schemas and runtime configuration</name>
  <files>tree-sitter-mcp/src/config/runtimeConfig.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/indexing/indexTypes.ts</files>
  <read_first>tree-sitter-mcp/src/config/runtimeConfig.ts, tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, .planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md</read_first>
  <action>Add persistent-index runtime configuration with the exact env override `TREE_SITTER_MCP_INDEX_DIR`, a concrete `indexSchemaVersion` value, and a default index root under the user's home directory. Create `tree-sitter-mcp/src/indexing/indexTypes.ts` with Zod schemas named `FreshnessStateSchema`, `WorkspaceIndexManifestSchema`, `IndexedFileSemanticRecordSchema`, and `WorkspaceIndexSummarySchema`. The freshness enum must be exactly `["fresh", "refreshed", "rebuilding", "degraded"]`, and `WorkspaceIndexSummarySchema` must include `enabled`, `storageMode`, `state`, `workspaceFingerprint`, `indexedFileCount`, `degradedFileCount`, `lastBuiltAt`, and `lastRefreshedAt`.</action>
  <verify>Read the new schemas and runtime config, then run `cd tree-sitter-mcp && npm test` after the unit tests in Task 3 are added.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/config/runtimeConfig.ts contains `TREE_SITTER_MCP_INDEX_DIR`
    - tree-sitter-mcp/src/config/runtimeConfig.ts contains `indexSchemaVersion`
    - tree-sitter-mcp/src/indexing/indexTypes.ts contains `FreshnessStateSchema = z.enum(["fresh", "refreshed", "rebuilding", "degraded"])`
    - tree-sitter-mcp/src/indexing/indexTypes.ts contains `WorkspaceIndexManifestSchema`
    - tree-sitter-mcp/src/workspace/workspaceState.ts contains `WorkspaceIndexSummarySchema`
  </acceptance_criteria>
  <done>Persistent-index configuration and shared schemas exist with exact freshness-state names and summary fields the rest of Phase 4 can build on.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workspace fingerprinting and disk-backed index storage</name>
  <files>tree-sitter-mcp/src/indexing/workspaceFingerprint.ts, tree-sitter-mcp/src/indexing/indexStorage.ts</files>
  <read_first>tree-sitter-mcp/src/config/runtimeConfig.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/indexing/indexTypes.ts</read_first>
  <action>Create `createWorkspaceFingerprint` in `workspaceFingerprint.ts` using a stable JSON payload of `{ root, exclusions, indexSchemaVersion }` hashed with `crypto.createHash("sha1")`. In `indexStorage.ts`, persist each workspace index under `<indexRootDir>/<workspaceFingerprint>/` with exact files `manifest.json` and `records.json`. `manifest.json` must include `schemaVersion`, `workspaceFingerprint`, `workspaceRoot`, `exclusions`, `lastBuiltAt`, `lastRefreshedAt`, `state`, `indexedFileCount`, and `degradedFiles`. `loadWorkspaceIndex` must reject manifests whose `schemaVersion` does not match the current runtime config and return a schema-mismatch result instead of silently loading them.</action>
  <verify>Use focused unit tests plus file reads under a temp index directory to prove the manifest and records files are created and schema mismatches are rejected.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/indexing/workspaceFingerprint.ts contains `createHash("sha1")`
    - tree-sitter-mcp/src/indexing/indexStorage.ts contains `manifest.json`
    - tree-sitter-mcp/src/indexing/indexStorage.ts contains `records.json`
    - tree-sitter-mcp/src/indexing/indexStorage.ts contains `schemaVersion`
    - tree-sitter-mcp/src/indexing/indexStorage.ts contains `loadWorkspaceIndex`
    - tree-sitter-mcp/src/indexing/indexStorage.ts contains `saveWorkspaceIndex`
  </acceptance_criteria>
  <done>Persistent indexes have a deterministic on-disk home, a stable workspace fingerprint, and explicit invalidation when schema expectations change.</done>
</task>

<task type="auto">
  <name>Task 3: Wire a semantic-index coordinator into server and workspace state</name>
  <files>tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/test/indexStorage.test.ts, tree-sitter-mcp/test/semanticIndexCoordinator.test.ts</files>
  <read_first>tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/indexing/indexTypes.ts, tree-sitter-mcp/src/indexing/indexStorage.ts, tree-sitter-mcp/src/indexing/workspaceFingerprint.ts</read_first>
  <action>Create `createSemanticIndexCoordinator` with concrete methods `loadPersistedIndex`, `replaceWorkspace`, `markBuilding`, `markFresh`, `markRefreshed`, `markDegraded`, `getSummary`, and `clear`. Extend `ServerContext` with a `semanticIndex` property initialized from runtime config and extend workspace state/summary objects with an `index` block shaped exactly like `WorkspaceIndexSummarySchema`. Add `indexStorage.test.ts` and `semanticIndexCoordinator.test.ts` to prove the coordinator reports `rebuilding` during bootstrap, transitions to `fresh` or `refreshed` when writes succeed, and reports `degraded` when the storage layer rejects an update.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the new coordinator and storage suites pass alongside the existing workspace tests.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/server/serverContext.ts contains `semanticIndex:`
    - tree-sitter-mcp/src/server/serverContext.ts contains `createSemanticIndexCoordinator`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `markDegraded`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `getSummary`
    - tree-sitter-mcp/test/indexStorage.test.ts contains `schemaVersion`
    - tree-sitter-mcp/test/semanticIndexCoordinator.test.ts contains `rebuilding`
    - tree-sitter-mcp/test/semanticIndexCoordinator.test.ts contains `degraded`
  </acceptance_criteria>
  <done>The server has a single source of truth for persistent-index lifecycle state, and tests lock the storage and coordinator contracts before search integration starts.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new index storage and coordinator suites
- [ ] Runtime config exposes `TREE_SITTER_MCP_INDEX_DIR` and `indexSchemaVersion`
- [ ] Persistent index summaries can report `fresh`, `refreshed`, `rebuilding`, and `degraded`
- [ ] Schema-version mismatches invalidate persisted data instead of reusing it silently
</verification>

<success_criteria>
- All tasks completed
- Persistent-index state has explicit schemas, storage boundaries, and invalidation rules
- ServerContext and workspace summaries can carry index metadata without breaking the read-only boundary
- Disk-backed workspace fingerprints make later cross-restart reuse possible
- Unit tests lock the lifecycle foundation before query integration begins
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistent-indexing-and-query-freshness/04-01-SUMMARY.md`
</output>
