---
phase: 04-persistent-indexing-and-query-freshness
plan: 03
type: execute
wave: 3
depends_on: [04-01, 04-02]
files_modified:
  - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/workspace/workspaceState.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/src/tools/setWorkspaceTool.ts
  - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
  - tree-sitter-mcp/src/tools/searchReferencesTool.ts
  - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/definitionTools.e2e.test.ts
  - tree-sitter-mcp/test/referenceTools.e2e.test.ts
  - tree-sitter-mcp/test/semanticTools.e2e.test.ts
  - tree-sitter-mcp/test/serverBootstrap.test.ts
autonomous: true
requirements: [PERF-02, PERF-03]
user_setup: []
must_haves:
  truths:
    - "Capabilities, health, workspace setup, and search results all expose machine-readable index and freshness metadata without cluttering the default text summaries."
    - "Degraded or partial-refresh situations surface warning diagnostics and never present stale file data as if it were fresh."
    - "Persistent-index behavior survives server restarts for the same workspace fingerprint when the index directory is reused."
    - "The server keeps its read-only MCP contract while making index mode and freshness state visible to agent callers."
  artifacts:
    - path: tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
      provides: Diagnostic codes for index build, refresh, degraded, and schema-mismatch states
      contains: index_refresh_failed
    - path: tree-sitter-mcp/src/server/toolRegistry.ts
      provides: Server-info payload that reports persistent index mode and current freshness state
      contains: eagerIndexing
    - path: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
      provides: Capability output with persistent-index status and coverage metadata
      contains: indexMode
    - path: tree-sitter-mcp/src/tools/getHealthTool.ts
      provides: Health output with persistent-index state, coverage, and degraded diagnostics
      contains: degraded
    - path: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
      provides: Definition-search payloads with freshness metadata
      contains: freshness
    - path: tree-sitter-mcp/src/tools/searchReferencesTool.ts
      provides: Reference-search payloads with freshness metadata and warning text for degraded states
      contains: freshness
    - path: tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
      provides: Regression coverage for reported index mode and freshness summaries
      contains: workspaceFingerprint
    - path: tree-sitter-mcp/test/referenceTools.e2e.test.ts
      provides: End-to-end coverage for restart reuse and degraded refresh warnings
      contains: TREE_SITTER_MCP_INDEX_DIR
  key_links:
    - from: tree-sitter-mcp/src/tools/getHealthTool.ts
      to: tree-sitter-mcp/src/workspace/workspaceState.ts
      via: health output serializes the same index summary stored on workspace state
      pattern: summarizeWorkspace
    - from: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
      to: tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
      via: degraded search results surface warning diagnostics through the shared diagnostic factory
      pattern: index_refresh_failed
    - from: tree-sitter-mcp/src/server/toolRegistry.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: server info reports current persistent-index state alongside parser mode for debugging
      pattern: getSummary
    - from: tree-sitter-mcp/test/referenceTools.e2e.test.ts
      to: tree-sitter-mcp/src/tools/searchReferencesTool.ts
      via: end-to-end tests prove freshness metadata and degraded warnings reach MCP callers over stdio
      pattern: freshness
---

<objective>
Expose freshness and persistent-index status clearly across the MCP tool surface and lock it down with end-to-end coverage.

Purpose: Satisfy the observability half of Phase 4 so agents can trust indexed results, understand degraded situations, and see whether persistent state was reused or refreshed.
Output: Updated diagnostics and tool payloads plus health/capability and end-to-end tests for restart reuse, refresh, and degraded behavior.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-01-PLAN.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-02-PLAN.md
@tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
@tree-sitter-mcp/src/server/toolRegistry.ts
@tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
@tree-sitter-mcp/src/tools/getHealthTool.ts
@tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
@tree-sitter-mcp/src/tools/searchReferencesTool.ts
@tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add diagnostic codes and index summaries to server metadata tools</name>
  <files>tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts</files>
  <read_first>tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts, tree-sitter-mcp/src/indexing/indexTypes.ts</read_first>
  <action>Add exact diagnostic codes `index_build_failed`, `index_refresh_failed`, `index_degraded`, and `index_schema_mismatch` to the shared diagnostic factory. Extend workspace summaries plus `tree_sitter_get_server_info`, `get_capabilities`, `get_health`, and `set_workspace` so they expose an `index` block containing `indexMode: "persistent_disk"`, `workspaceFingerprint`, `state`, `indexedFileCount`, `degradedFileCount`, `lastBuiltAt`, and `lastRefreshedAt`. Keep the existing `parserMode` field for backward compatibility, but set `eagerIndexing` in server info based on whether `set_workspace` now blocks on index build.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the capability/health/bootstrap suites pass with the new index metadata fields.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts contains `index_build_failed`
    - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts contains `index_refresh_failed`
    - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts contains `indexMode`
    - tree-sitter-mcp/src/tools/getHealthTool.ts contains `workspaceFingerprint`
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `lastBuiltAt`
    - tree-sitter-mcp/src/server/toolRegistry.ts contains `eagerIndexing`
  </acceptance_criteria>
  <done>Agents can inspect persistent-index mode, coverage, and degraded state through the server metadata tools before running deeper searches.</done>
</task>

<task type="auto">
  <name>Task 2: Add freshness metadata and warning behavior to search tool payloads</name>
  <files>tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts</files>
  <read_first>tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, .planning/phases/04-persistent-indexing-and-query-freshness/04-CONTEXT.md</read_first>
  <action>Add a `freshness` object to each workspace-wide search payload with exact fields `state`, `checkedAt`, `refreshedFiles`, `degradedFiles`, and `workspaceFingerprint`. Default text should remain short for `fresh` results, append `Refreshed N file(s) before searching.` when `state` is `refreshed`, and surface warning-style text only when `state` is `degraded` or `rebuilding`. When a result set is partial because a changed file failed to refresh, keep the search result successful, attach warning diagnostics using `index_refresh_failed` or `index_degraded`, and never include matches from the failed file in the returned results array.</action>
  <verify>Inspect structured payloads in E2E tests and confirm the search tools now expose `freshness` plus warning behavior for degraded refreshes.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts contains `freshness`
    - tree-sitter-mcp/src/tools/searchReferencesTool.ts contains `freshness`
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `freshness`
    - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts contains `Refreshed N file(s) before searching.`
    - tree-sitter-mcp/src/tools/searchReferencesTool.ts contains `index_degraded`
  </acceptance_criteria>
  <done>Search callers receive structured freshness data by default and clear warnings whenever results are partial or degraded.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end coverage for restart reuse, refresh, and degraded results</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts, tree-sitter-mcp/test/serverBootstrap.test.ts</files>
  <read_first>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts, tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/src/config/runtimeConfig.ts</read_first>
  <action>Update the E2E suites to set `TREE_SITTER_MCP_INDEX_DIR` to a temp directory and verify four exact scenarios: (1) `set_workspace` builds the initial index before the first workspace-wide search returns, (2) restarting the server with the same workspace and index directory reuses the persisted index for the same `workspaceFingerprint`, (3) editing a supported file causes the next search to report `freshness.state === "refreshed"`, and (4) breaking a changed file causes the next search to report `freshness.state === "degraded"` plus warning diagnostics while omitting stale matches from that file. Update the capability/health/bootstrap suites to assert the new index metadata is present and stable.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm all updated E2E and metadata suites pass with a temp persistent-index directory.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `workspaceFingerprint`
    - tree-sitter-mcp/test/definitionTools.e2e.test.ts contains `TREE_SITTER_MCP_INDEX_DIR`
    - tree-sitter-mcp/test/referenceTools.e2e.test.ts contains `freshness.state === "degraded"`
    - tree-sitter-mcp/test/semanticTools.e2e.test.ts contains `freshness.state === "refreshed"`
    - tree-sitter-mcp/test/serverBootstrap.test.ts contains `eagerIndexing`
  </acceptance_criteria>
  <done>Phase 4 has executable proof that persistent-index status is visible, reused across restarts, refreshed on change, and degraded safely when refresh fails.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes updated bootstrap, capability/health, and search E2E coverage
- [ ] `get_capabilities`, `get_health`, `set_workspace`, and `tree_sitter_get_server_info` all expose persistent-index metadata
- [ ] Search payloads expose a `freshness` block and warning diagnostics for degraded partial results
- [ ] Restart reuse, refreshed state, and degraded stale-file exclusion are all covered by automated tests
</verification>

<success_criteria>
- All tasks completed
- Agent callers can see index mode, freshness state, and degraded coverage without reading logs
- Search tools remain concise by default but become explicit when refresh or degraded states matter
- Persistent-index reuse across restarts is proven in end-to-end tests
- Phase 4 satisfies both the performance and observability halves of the milestone scope
</success_criteria>

<output>
After completion, create `.planning/phases/04-persistent-indexing-and-query-freshness/04-03-SUMMARY.md`
</output>
