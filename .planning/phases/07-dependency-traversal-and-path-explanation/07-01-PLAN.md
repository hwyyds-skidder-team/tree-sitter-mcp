---
phase: 07-dependency-traversal-and-path-explanation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/dependencies/dependencyTypes.ts
  - tree-sitter-mcp/src/dependencies/dependencyFilters.ts
  - tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts
  - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
  - tree-sitter-mcp/test/dependencyFilters.test.ts
autonomous: true
requirements: [DEPS-02, DEPS-03, DEPS-04]
user_setup: []
must_haves:
  truths:
    - "Dependency analysis has its own additive request/result contract instead of silently changing the shipped Phase 6 relationship-view payload."
    - "Traversal depth and relationship-kind bounds are normalized deterministically before any backend traversal starts."
    - "Every explanation path step carries stable symbol and evidence attribution so returned results stay actionable inside local workspaces."
  artifacts:
    - path: tree-sitter-mcp/src/dependencies/dependencyTypes.ts
      provides: Dependency-analysis request/result schemas plus explanation-path step contracts
      contains: DependencyPathStepSchema
    - path: tree-sitter-mcp/src/dependencies/dependencyFilters.ts
      provides: Shared normalization for dependency workspace/language/kind/depth filters
      exports:
        - normalizeDependencyFilters
    - path: tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts
      provides: Additive dependency_analysis query registration
      exports:
        - listDependencyQueryTypes
    - path: tree-sitter-mcp/test/dependencyFilters.test.ts
      provides: Focused coverage for defaults, dedupe, workspace validation, and depth bounds
      contains: dependency_depth_invalid
  key_links:
    - from: tree-sitter-mcp/src/dependencies/dependencyTypes.ts
      to: tree-sitter-mcp/src/definitions/definitionTypes.ts
      via: explanation paths must reuse the existing definition match contract for every symbol endpoint
      pattern: DefinitionMatchSchema
    - from: tree-sitter-mcp/src/dependencies/dependencyTypes.ts
      to: tree-sitter-mcp/src/references/referenceTypes.ts
      via: every path step must preserve the concrete indexed evidence that justified the hop
      pattern: ReferenceMatchSchema
    - from: tree-sitter-mcp/src/dependencies/dependencyFilters.ts
      to: tree-sitter-mcp/src/relationships/relationshipFilters.ts
      via: dependency traversal must inherit the same workspace/language/kind normalization conventions as relationship views
      pattern: DEFAULT_RELATIONSHIP_KINDS
    - from: tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts
      to: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
      via: the new semantic surface becomes discoverable through the same additive query-type inventory used elsewhere
      pattern: dependency_analysis
---

<objective>
Lock the Phase 7 dependency-analysis contract before traversal code lands.

Purpose: Define an additive query surface for bounded multi-hop dependency analysis with explanation paths and stable attribution, so later plans can implement the backend without inventing schemas or filter semantics ad hoc.
Output: Dependency-analysis schemas, filter normalization, a query-catalog entry, a new diagnostic code for depth validation, and focused regression tests.
</objective>

<execution_context>
@/home/hwyyds/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-dependency-traversal-and-path-explanation/07-RESEARCH.md
@.planning/phases/06-relationship-views-and-impact-discovery/06-01-SUMMARY.md
@.planning/phases/06-relationship-views-and-impact-discovery/06-02-SUMMARY.md
@tree-sitter-mcp/src/relationships/relationshipTypes.ts
@tree-sitter-mcp/src/relationships/relationshipFilters.ts
@tree-sitter-mcp/src/definitions/definitionTypes.ts
@tree-sitter-mcp/src/references/referenceTypes.ts
@tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts

<interfaces>
From `tree-sitter-mcp/src/relationships/relationshipTypes.ts`:
```typescript
export const RelationshipKindSchema = z.enum([
  "incoming_call",
  "incoming_reference",
  "outgoing_call",
  "outgoing_reference",
]);

export const RelationshipEdgeSchema = z.object({
  relationshipKind: RelationshipKindSchema,
  hopCount: z.number().int().min(1).max(2),
  relatedSymbol: DefinitionMatchSchema,
  evidence: ReferenceMatchSchema,
});
```

From `tree-sitter-mcp/src/definitions/definitionTypes.ts`:
```typescript
export const DefinitionMatchSchema = z.object({
  name: z.string().min(1),
  kind: SymbolKindSchema,
  languageId: z.string().min(1),
  workspaceRoot: z.string().min(1),
  filePath: z.string().min(1),
  relativePath: z.string().min(1),
  range: SourceRangeSchema,
  selectionRange: SourceRangeSchema,
  containerName: z.string().nullable(),
  snippet: z.string(),
});
```

From `tree-sitter-mcp/src/references/referenceTypes.ts`:
```typescript
export const ReferenceMatchSchema = z.object({
  name: z.string().min(1),
  referenceKind: ReferenceKindSchema,
  symbolKind: SymbolKindSchema.nullable(),
  languageId: z.string().min(1),
  workspaceRoot: z.string().min(1),
  filePath: z.string().min(1),
  relativePath: z.string().min(1),
  range: SourceRangeSchema,
  selectionRange: SourceRangeSchema,
  containerName: z.string().nullable(),
  snippet: z.string(),
  enclosingContext: EnclosingContextSchema.nullable().optional(),
  contextSnippet: ContextSnippetSchema.nullable().optional(),
});
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define dependency-analysis schemas and explanation-path contracts</name>
  <files>tree-sitter-mcp/src/dependencies/dependencyTypes.ts</files>
  <read_first>tree-sitter-mcp/src/relationships/relationshipTypes.ts, tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/results/paginateResults.ts, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/phases/07-dependency-traversal-and-path-explanation/07-RESEARCH.md</read_first>
  <action>Create `tree-sitter-mcp/src/dependencies/dependencyTypes.ts` with the exact additive Phase 7 contract. Define `DependencyDirectionSchema` as `incoming | outgoing`. Define `DependencyPathStepSchema` with the exact required fields `relationshipKind`, `fromSymbol`, `toSymbol`, and `evidence`, where `fromSymbol` and `toSymbol` both reuse `DefinitionMatchSchema` and `evidence` reuses `ReferenceMatchSchema`. Define `DependencyResultSchema` with the exact required fields `symbol`, `direction`, `depth`, and `path`, where `depth` is `1..4` and `path` is a non-empty array of `DependencyPathStepSchema`. Define `DependencyFilterSchema` with exact fields `workspaceRoots`, `language`, `relationshipKinds`, `maxDepth`, `limit`, and `offset`. Define `DependencyAnalysisRequestSchema` with a seed via exact additive `symbol` or `lookup` plus those same filters. Add `ValidatedDependencyAnalysisRequestSchema` that rejects requests without `symbol` or `lookup`. Define `DependencyAnalysisResultSchema` with the exact fields `target`, `filters`, `results`, and `pagination`. Keep this contract additive — do not modify `relationshipTypes.ts` or widen `get_relationship_view` for Phase 7.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `DependencyDirectionSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `DependencyPathStepSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `fromSymbol: DefinitionMatchSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `toSymbol: DefinitionMatchSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `evidence: ReferenceMatchSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `depth: z.number().int().min(1).max(4)`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `ValidatedDependencyAnalysisRequestSchema`
    - tree-sitter-mcp/src/dependencies/dependencyTypes.ts contains `Provide a dependency seed via symbol or lookup.`
  </acceptance_criteria>
  <done>Phase 7 has a concrete schema language for bounded dependency results and explanation paths.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize dependency filters and add a dedicated depth diagnostic</name>
  <files>tree-sitter-mcp/src/dependencies/dependencyFilters.ts, tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts</files>
  <read_first>tree-sitter-mcp/src/dependencies/dependencyTypes.ts, tree-sitter-mcp/src/relationships/relationshipFilters.ts, tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts, tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/languages/languageRegistry.ts</read_first>
  <action>Create `tree-sitter-mcp/src/dependencies/dependencyFilters.ts` with the exact exports `DEFAULT_DEPENDENCY_RELATIONSHIP_KINDS`, `normalizeDependencyFilters`, and `matchesDependencyResultFilters`. Reuse the canonical relationship-kind ordering from Phase 6, normalize `workspaceRoots` against the configured root list, lowercase and validate `language` through the language registry, default `relationshipKinds` to all four canonical values, default `maxDepth` to `2`, default `limit` to `50`, default `offset` to `0`, and reject depth values outside `1..4`. Extend `tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts` with the exact new code `dependency_depth_invalid`; do not reuse `relationship_depth_invalid` for this surface. Keep filter semantics intentionally narrow to `workspaceRoots`, `language`, `relationshipKinds`, `maxDepth`, `limit`, and `offset`.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `export const DEFAULT_DEPENDENCY_RELATIONSHIP_KINDS`
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `export function normalizeDependencyFilters`
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `export function matchesDependencyResultFilters`
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `maxDepth: 2`
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `limit: 50`
    - tree-sitter-mcp/src/dependencies/dependencyFilters.ts contains `offset: 0`
    - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts contains `dependency_depth_invalid`
  </acceptance_criteria>
  <done>Dependency analysis gets one reusable normalization path with explicit bounds and diagnostics before traversal code exists.</done>
</task>

<task type="auto">
  <name>Task 3: Register `dependency_analysis` and lock the contract with focused tests</name>
  <files>tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts, tree-sitter-mcp/test/dependencyFilters.test.ts</files>
  <read_first>tree-sitter-mcp/src/dependencies/dependencyTypes.ts, tree-sitter-mcp/src/dependencies/dependencyFilters.ts, tree-sitter-mcp/src/queries/relationshipQueryCatalog.ts, tree-sitter-mcp/test/relationshipFilters.test.ts</read_first>
  <action>Create `tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts` with exact export `listDependencyQueryTypes` that returns `dependency_analysis`. Add `tree-sitter-mcp/test/dependencyFilters.test.ts` to prove the Phase 7 contract before backend implementation: defaults to all four relationship kinds, defaults `maxDepth` to `2`, rejects depth `0` and `5` with diagnostic code `dependency_depth_invalid`, dedupes relationship kinds into canonical order, rejects unsupported workspace roots and unsupported languages, and preserves `workspaceRoots`, `limit`, and `offset` in the normalized filter output.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build &amp;&amp; tsx --test test/dependencyFilters.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts contains `dependency_analysis`
    - tree-sitter-mcp/src/queries/dependencyQueryCatalog.ts contains `export function listDependencyQueryTypes`
    - tree-sitter-mcp/test/dependencyFilters.test.ts contains `dependency_depth_invalid`
    - tree-sitter-mcp/test/dependencyFilters.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/dependencyFilters.test.ts contains `relationshipKinds`
    - tree-sitter-mcp/test/dependencyFilters.test.ts contains `maxDepth`
  </acceptance_criteria>
  <done>The additive Phase 7 contract is registered and test-locked before any traversal backend or tool wiring begins.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm run build && tsx --test test/dependencyFilters.test.ts` passes
- [ ] Phase 7 has an additive `dependency_analysis` contract rather than a silent expansion of `get_relationship_view`
- [ ] Explanation-path steps guarantee stable symbol and evidence attribution via existing definition/reference primitives
- [ ] Depth and relationship-kind bounds are normalized deterministically before traversal begins
</verification>

<success_criteria>
- All tasks completed
- Phase 7 starts from explicit schemas and diagnostics instead of backend-invented payloads
- Future plans can implement traversal and tool wiring against stable request/result contracts
- Requirement coverage for bounded traversal, explanation paths, and attribution is represented directly in the contract layer
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-traversal-and-path-explanation/07-01-SUMMARY.md`
</output>
