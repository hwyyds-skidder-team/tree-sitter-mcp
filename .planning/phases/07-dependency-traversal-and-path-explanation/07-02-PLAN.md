---
phase: 07-dependency-traversal-and-path-explanation
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - tree-sitter-mcp/src/relationships/relationshipTraversal.ts
  - tree-sitter-mcp/src/relationships/getRelationshipView.ts
  - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
  - tree-sitter-mcp/test/dependencyAnalysis.test.ts
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03, DEPS-04]
user_setup: []
must_haves:
  truths:
    - "Dependency analysis can traverse beyond Phase 6's one-extra-hop neighborhood while remaining bounded by explicit depth and relationship-kind filters."
    - "Every returned dependency result includes a canonical explanation path back to the requested seed symbol."
    - "Traversal reuses the same direct-edge semantics as `get_relationship_view`, so workspace attribution and evidence shaping do not drift between the two surfaces."
  artifacts:
    - path: tree-sitter-mcp/src/relationships/relationshipTraversal.ts
      provides: Shared direct-edge collection and ordering helpers used by both relationship view and dependency analysis
      exports:
        - collectDirectRelationshipEdges
    - path: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      provides: Bounded multi-hop dependency traversal with canonical path selection
      exports:
        - getDependencyAnalysis
    - path: tree-sitter-mcp/test/dependencyAnalysis.test.ts
      provides: In-process proof for traversal depth, path explanation, cycle handling, and attribution
      contains: path.length
  key_links:
    - from: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      to: tree-sitter-mcp/src/relationships/relationshipTraversal.ts
      via: Phase 7 must reuse the exact one-hop edge collector that powers Phase 6 instead of reclassifying calls/references a second way
      pattern: collectDirectRelationshipEdges
    - from: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      to: tree-sitter-mcp/src/definitions/resolveDefinition.ts
      via: the seed and all follow-up symbol lookups must continue to resolve through the shared definition pipeline
      pattern: resolveDefinition
    - from: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: dependency analysis must keep using freshness-checked indexed records rather than reparsing files on demand
      pattern: getFreshRecords
    - from: tree-sitter-mcp/src/relationships/getRelationshipView.ts
      to: tree-sitter-mcp/src/relationships/relationshipTraversal.ts
      via: the existing Phase 6 tool must preserve its current payload by delegating only internal edge collection to the shared helper
      pattern: relationshipTraversal
---

<objective>
Implement the Phase 7 backend on top of shared relationship primitives.

Purpose: Refactor one-hop relationship expansion into a reusable helper, then build a bounded multi-hop dependency-analysis backend that returns unique reachable symbols with canonical explanation paths and stable attribution.
Output: A shared relationship traversal helper, a new dependency-analysis backend, and focused backend tests for depth bounds, path selection, and cycle handling.
</objective>

<execution_context>
@/home/hwyyds/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-dependency-traversal-and-path-explanation/07-RESEARCH.md
@.planning/phases/07-dependency-traversal-and-path-explanation/07-01-PLAN.md
@tree-sitter-mcp/src/dependencies/dependencyTypes.ts
@tree-sitter-mcp/src/dependencies/dependencyFilters.ts
@tree-sitter-mcp/src/relationships/getRelationshipView.ts
@tree-sitter-mcp/src/definitions/resolveDefinition.ts
@tree-sitter-mcp/src/references/searchReferences.ts
@tree-sitter-mcp/test/relationshipPipeline.test.ts

<interfaces>
From `tree-sitter-mcp/src/relationships/getRelationshipView.ts`:
```typescript
export interface GetRelationshipViewResult {
  target: DefinitionMatch | null;
  filters: RelationshipFilters;
  edges: RelationshipEdge[];
  pagination: Pagination;
  freshness: SearchFreshness;
  diagnostic: Diagnostic | null;
  diagnostics: Diagnostic[];
  searchedFiles: number;
  matchedFiles: number;
}

export async function getRelationshipView(
  context: ServerContext,
  request: RelationshipViewRequest,
): Promise<GetRelationshipViewResult>;
```

From `tree-sitter-mcp/src/definitions/resolveDefinition.ts`:
```typescript
export interface ResolveDefinitionRequest {
  symbol?: DefinitionSymbolDescriptor;
  lookup?: DefinitionLookupRequest;
}

export async function resolveDefinition(
  context: ServerContext,
  request: ResolveDefinitionRequest,
): Promise<ResolveDefinitionResult>;
```

From `tree-sitter-mcp/src/dependencies/dependencyTypes.ts` (created by 07-01):
```typescript
export const DependencyResultSchema = z.object({
  symbol: DefinitionMatchSchema,
  direction: DependencyDirectionSchema,
  depth: z.number().int().min(1).max(4),
  path: z.array(DependencyPathStepSchema).min(1),
});
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extract shared direct-edge traversal helpers without changing Phase 6 behavior</name>
  <files>tree-sitter-mcp/src/relationships/relationshipTraversal.ts, tree-sitter-mcp/src/relationships/getRelationshipView.ts</files>
  <read_first>tree-sitter-mcp/src/relationships/getRelationshipView.ts, tree-sitter-mcp/src/relationships/relationshipTypes.ts, tree-sitter-mcp/src/relationships/relationshipFilters.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/test/relationshipPipeline.test.ts, tree-sitter-mcp/test/relationshipTools.e2e.test.ts</read_first>
  <behavior>
    - Test 1: Existing relationship pipeline tests still return the same direct incoming/outgoing edges after helper extraction.
    - Test 2: Existing relationship E2E tests still pass without any `get_relationship_view` payload changes.
  </behavior>
  <action>Create `tree-sitter-mcp/src/relationships/relationshipTraversal.ts` and move the one-hop collection logic out of `getRelationshipView.ts` into exact reusable exports `collectDirectRelationshipEdges`, `createRelationshipEdgeKey`, and `sortRelationshipEdges`. Move the current incoming/outgoing edge collection, same-file → same-workspace → global resolution preference, self/unresolved/ownerless diagnostics, and stable edge-key logic into that helper module. Update `tree-sitter-mcp/src/relationships/getRelationshipView.ts` to import those helpers and preserve the exact Phase 6 public payload shape, default behavior, and freshness handling; do not change `get_relationship_view` request or response fields in this plan.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build &amp;&amp; tsx --test test/relationshipPipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/relationships/relationshipTraversal.ts contains `export async function collectDirectRelationshipEdges`
    - tree-sitter-mcp/src/relationships/relationshipTraversal.ts contains `export function createRelationshipEdgeKey`
    - tree-sitter-mcp/src/relationships/relationshipTraversal.ts contains `export function sortRelationshipEdges`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `from "./relationshipTraversal.js"`
    - tree-sitter-mcp/src/relationships/getRelationshipView.ts contains `collectDirectRelationshipEdges`
  </acceptance_criteria>
  <done>Phase 6 and Phase 7 can share one direct-edge source of truth without changing the shipped Phase 6 contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded multi-hop dependency analysis with canonical shortest paths</name>
  <files>tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts</files>
  <read_first>tree-sitter-mcp/src/dependencies/dependencyTypes.ts, tree-sitter-mcp/src/dependencies/dependencyFilters.ts, tree-sitter-mcp/src/relationships/relationshipTraversal.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/src/results/paginateResults.ts</read_first>
  <behavior>
    - Test 1: `maxDepth: 3` returns third-hop dependencies while `maxDepth: 1` does not.
    - Test 2: A symbol reachable through two paths returns one canonical shortest explanation path.
    - Test 3: Every explanation step preserves `workspaceRoot`, `relativePath`, and `relationshipKind` metadata.
  </behavior>
  <action>Create `tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts` with exact export `getDependencyAnalysis`. Resolve the seed symbol once, normalize Phase 7 filters, fetch one fresh-index snapshot for the request, and run breadth-first traversal from the seed up to `maxDepth` `1..4`. At each frontier expansion, call `collectDirectRelationshipEdges` with `maxDepth` fixed to `1` semantics so Phase 7 reuses Phase 6 direct-edge classification instead of widening it. Store results by the exact key `{ direction, workspaceRoot, relativePath, selectionRange.start.offset }`, where `direction` is derived from the first step in the path. When a symbol is discovered multiple times, keep only the first-discovered path from BFS order so the explanation stays the canonical shortest path. Sort final results by `depth`, `direction` (`incoming` before `outgoing`), configured workspace-root order, `relativePath`, and `selectionRange.start.offset`, then apply `limit`/`offset`. Return per-result `path` arrays whose steps contain exact `fromSymbol`, `toSymbol`, `relationshipKind`, and `evidence` values. Preserve actionable diagnostics and combined freshness in the final payload; never exceed `maxDepth`, and never recurse infinitely on cycles.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `export async function getDependencyAnalysis`
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `collectDirectRelationshipEdges`
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `maxDepth`
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `path:`
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `direction`
    - tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts contains `selectionRange.start.offset`
  </acceptance_criteria>
  <done>The backend can return unique reachable symbols plus an explicit explanation path for each one, bounded by caller-supplied depth and kind filters.</done>
</task>

<task type="auto">
  <name>Task 3: Lock backend traversal, path explanation, and cycle handling with focused tests</name>
  <files>tree-sitter-mcp/test/dependencyAnalysis.test.ts</files>
  <read_first>tree-sitter-mcp/test/relationshipPipeline.test.ts, tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts, tree-sitter-mcp/src/relationships/relationshipTraversal.ts, tree-sitter-mcp/src/dependencies/dependencyFilters.ts</read_first>
  <action>Create `tree-sitter-mcp/test/dependencyAnalysis.test.ts` with in-process fixtures that prove the new backend before MCP tool wiring. Include an exact three-hop chain, at least one incoming path, one outgoing path, one cycle, and one symbol reachable by both a short path and a longer alternative path. Assert these exact behaviors: `maxDepth: 1` excludes deeper symbols, `maxDepth: 3` includes the third-hop symbol with `path.length === 3`, `relationshipKinds` filtering removes paths that require excluded edge kinds, every path step preserves `fromSymbol.workspaceRoot`, `toSymbol.workspaceRoot`, and `evidence.workspaceRoot`, and the duplicated target keeps only the canonical shortest path. Keep this suite focused on the dependency backend, not stdio transport.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build &amp;&amp; tsx --test test/dependencyAnalysis.test.ts test/relationshipPipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `maxDepth: 3`
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `path.length`
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `relationshipKinds`
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `incoming`
    - tree-sitter-mcp/test/dependencyAnalysis.test.ts contains `outgoing`
  </acceptance_criteria>
  <done>The Phase 7 backend is proven for bounded traversal, explanation paths, attribution, and cycle-safe shortest-path behavior before MCP exposure.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm run build && tsx --test test/dependencyAnalysis.test.ts test/relationshipPipeline.test.ts` passes
- [ ] Dependency analysis reaches multi-hop symbols beyond Phase 6 while remaining bounded by `maxDepth`
- [ ] Each returned dependency result carries exactly one canonical explanation path back to the seed
- [ ] Phase 6 relationship traversal continues to use the same direct-edge semantics after helper extraction
</verification>

<success_criteria>
- All tasks completed
- The project has one reusable direct-edge collector shared by relationship view and dependency analysis
- Phase 7 backend results are symbol-oriented, attributable, and path-explained rather than raw edge dumps
- Requirement coverage for multi-hop traversal, bounds, path explanation, and stable attribution is implemented in backend code and tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-traversal-and-path-explanation/07-02-SUMMARY.md`
</output>
