---
phase: 07-dependency-traversal-and-path-explanation
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/test/dependencyTools.e2e.test.ts
  - tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts
  - tree-sitter-mcp/test/serverBootstrap.test.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/searchIndexIntegration.test.ts
autonomous: true
requirements: [DEPS-01, DEPS-02, DEPS-03, DEPS-04]
user_setup: []
must_haves:
  truths:
    - "Agents can call a read-only dependency-analysis MCP tool and receive multi-hop results with explanation paths over stdio."
    - "Dependency-analysis results remain attributable and deterministic across multiple workspace roots, even when names and relative paths overlap."
    - "Dependency-analysis discovery surfaces appear in bootstrap, capabilities, and health metadata just like the existing search tools."
    - "Dependency-analysis responses inherit freshness/degraded diagnostics instead of serving stale paths after file changes."
  artifacts:
    - path: tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts
      provides: Read-only MCP exposure for dependency analysis
      contains: get_dependency_analysis
    - path: tree-sitter-mcp/test/dependencyTools.e2e.test.ts
      provides: Single-root stdio proof for explanation paths and read-only behavior
      contains: path.length
    - path: tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts
      provides: Federated proof for workspace attribution and duplicate-name disambiguation
      contains: workspaceRoots
    - path: tree-sitter-mcp/test/searchIndexIntegration.test.ts
      provides: Freshness/degraded regression coverage for dependency analysis
      contains: get_dependency_analysis
  key_links:
    - from: tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts
      to: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      via: the MCP tool must be a thin read-only adapter over the shared backend, not a second traversal implementation
      pattern: getDependencyAnalysis
    - from: tree-sitter-mcp/test/dependencyTools.e2e.test.ts
      to: tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts
      via: the compiled stdio suite must prove explanation-path payloads from the actual MCP boundary
      pattern: get_dependency_analysis
    - from: tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts
      to: tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
      via: federated fixtures must prove path steps keep correct `workspaceRoot` attribution on both symbol endpoints and evidence
      pattern: workspaceRoot
    - from: tree-sitter-mcp/test/searchIndexIntegration.test.ts
      to: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      via: dependency analysis must refresh or degrade with the same explicit freshness guarantees as existing indexed search tools
      pattern: freshness
---

<objective>
Expose Phase 7 as a read-only MCP workflow and validate it end to end.

Purpose: Wire dependency analysis into the stdio server, advertise it through the normal metadata surfaces, and prove single-root, federated, and freshness-aware behavior from the real MCP boundary.
Output: A `get_dependency_analysis` tool, metadata wiring, dedicated E2E suites, and bootstrap/health/freshness regression coverage.
</objective>

<execution_context>
@/home/hwyyds/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-dependency-traversal-and-path-explanation/07-RESEARCH.md
@.planning/phases/07-dependency-traversal-and-path-explanation/07-02-PLAN.md
@tree-sitter-mcp/src/dependencies/dependencyTypes.ts
@tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts
@tree-sitter-mcp/src/tools/getRelationshipViewTool.ts
@tree-sitter-mcp/src/server/toolRegistry.ts
@tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
@tree-sitter-mcp/src/tools/getHealthTool.ts
@tree-sitter-mcp/test/relationshipTools.e2e.test.ts
@tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts

<interfaces>
From `tree-sitter-mcp/src/dependencies/dependencyTypes.ts` (created by 07-01):
```typescript
export const DependencyAnalysisRequestSchema = z.object({
  symbol: RelationshipSeedSchema.optional(),
  lookup: RelationshipSeedSchema.optional(),
  workspaceRoots: z.array(z.string().min(1)).min(1).optional(),
  language: z.string().min(1).optional(),
  relationshipKinds: z.array(RelationshipKindSchema).optional(),
  maxDepth: z.number().int().min(1).max(4).optional(),
  limit: z.number().int().positive().max(200).optional(),
  offset: z.number().int().nonnegative().optional(),
});
```

From `tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts` (created by 07-02):
```typescript
export async function getDependencyAnalysis(
  context: ServerContext,
  request: DependencyAnalysisRequest,
): Promise<GetDependencyAnalysisResult>;
```

From `tree-sitter-mcp/src/tools/getRelationshipViewTool.ts`:
```typescript
const GetRelationshipViewOutputSchema = z.object({
  workspaceRoot: z.string().nullable(),
  workspaceRoots: z.array(z.string()),
  seed: DefinitionMatchSchema.nullable(),
  results: z.array(RelationshipEdgeSchema),
  searchedFiles: z.number().int().nonnegative(),
  matchedFiles: z.number().int().nonnegative(),
  pagination: PaginationSchema,
  workspaceBreakdown: z.array(WorkspaceBreakdownSchema),
  freshness: SearchFreshnessSchema,
  diagnostic: DiagnosticSchema.nullable(),
  diagnostics: z.array(DiagnosticSchema),
});
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Wire `get_dependency_analysis` as a thin read-only MCP adapter</name>
  <files>tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts</files>
  <read_first>tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts, tree-sitter-mcp/src/dependencies/dependencyTypes.ts, tree-sitter-mcp/src/tools/getRelationshipViewTool.ts, tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/results/workspaceBreakdown.ts</read_first>
  <behavior>
    - Test 1: Capabilities and health advertise `dependency_analysis` and `get_dependency_analysis`.
    - Test 2: The tool returns workspace-aware results, pagination, diagnostics, and freshness without mutating the backend contract.
  </behavior>
  <action>Create `tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts` and register the exact tool name `get_dependency_analysis`. Use `DependencyAnalysisRequestSchema` as input validation, keep `readOnlyHint: true`, and return the exact output fields `workspaceRoot`, `workspaceRoots`, `seed`, `results`, `searchedFiles`, `matchedFiles`, `pagination`, `workspaceBreakdown`, `freshness`, `diagnostic`, and `diagnostics`. Shape `workspaceBreakdown` with the existing `createWorkspaceBreakdown` helper using returned dependency symbols, not raw edges. Make the text response mention the result count, matched-file count, selected-workspace count, and requested depth. Update `tree-sitter-mcp/src/server/toolRegistry.ts` to register the tool, and update `tree-sitter-mcp/src/tools/getCapabilitiesTool.ts` plus `tree-sitter-mcp/src/tools/getHealthTool.ts` so both `supportedQueryTypes` and `toolNames` advertise `dependency_analysis` and `get_dependency_analysis`. Keep `get_relationship_view` unchanged and additive.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts contains `SearchFreshnessSchema`
    - tree-sitter-mcp/src/server/toolRegistry.ts contains `registerGetDependencyAnalysisTool`
    - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts contains `dependency_analysis`
    - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/src/tools/getHealthTool.ts contains `dependency_analysis`
    - tree-sitter-mcp/src/tools/getHealthTool.ts contains `get_dependency_analysis`
  </acceptance_criteria>
  <done>Phase 7 is exposed through one additive read-only MCP tool and standard discovery metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Prove single-root and federated dependency analysis over stdio</name>
  <files>tree-sitter-mcp/test/dependencyTools.e2e.test.ts, tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts</files>
  <read_first>tree-sitter-mcp/test/relationshipTools.e2e.test.ts, tree-sitter-mcp/test/multiWorkspaceRelationships.e2e.test.ts, tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts, tree-sitter-mcp/src/dependencies/getDependencyAnalysis.ts</read_first>
  <action>Create `tree-sitter-mcp/test/dependencyTools.e2e.test.ts` with a realistic single-root stdio fixture containing at least a three-hop outgoing chain, an incoming dependency, and a cycle. Assert this exact read-only flow: `set_workspace`, resolve or search for the seed definition, call `get_dependency_analysis` with `maxDepth: 3`, and verify a third-hop result exists with `depth === 3` and `path.length === 3`. Assert every path step contains `fromSymbol.workspaceRoot`, `toSymbol.workspaceRoot`, `evidence.workspaceRoot`, `relativePath`, and `relationshipKind`, and prove the temp workspace snapshot is unchanged before and after the tool sequence. Create `tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts` with two or more roots that reuse symbol names and relative paths. Assert duplicate seeds are disambiguated by `workspaceRoot`, `workspaceRoots` filtering excludes the other root entirely, and explanation paths never hop into the wrong workspace just because names match.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build &amp;&amp; tsx --test test/dependencyTools.e2e.test.ts test/multiWorkspaceDependencies.e2e.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/dependencyTools.e2e.test.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/test/dependencyTools.e2e.test.ts contains `path.length`
    - tree-sitter-mcp/test/dependencyTools.e2e.test.ts contains `maxDepth: 3`
    - tree-sitter-mcp/test/dependencyTools.e2e.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/multiWorkspaceDependencies.e2e.test.ts contains `workspaceRoot`
  </acceptance_criteria>
  <done>Dependency analysis is proven over the real stdio MCP boundary for both single-root and federated repositories.</done>
</task>

<task type="auto">
  <name>Task 3: Extend bootstrap, metadata, and freshness coverage for dependency analysis</name>
  <files>tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts</files>
  <read_first>tree-sitter-mcp/test/serverBootstrap.test.ts, tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts, tree-sitter-mcp/src/tools/getDependencyAnalysisTool.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts</read_first>
  <action>Update `tree-sitter-mcp/test/serverBootstrap.test.ts` so the compiled server lists `get_dependency_analysis` and its input schema fields include `maxDepth`, `relationshipKinds`, `workspaceRoots`, `limit`, and `offset`. Update `tree-sitter-mcp/test/capabilitiesAndHealth.test.ts` so `supportedQueryTypes` includes `dependency_analysis` and `toolNames` includes `get_dependency_analysis` in both single-root and multi-root health checks. Extend `tree-sitter-mcp/test/searchIndexIntegration.test.ts` with a dependency-analysis scenario that edits a file inside a multi-hop chain, proves refreshed paths change after the edit, and proves degraded parse failures return freshness warnings/diagnostics instead of stale explanation paths.</action>
  <verify>
    <automated>cd tree-sitter-mcp &amp;&amp; npm run build &amp;&amp; tsx --test test/serverBootstrap.test.ts test/capabilitiesAndHealth.test.ts test/searchIndexIntegration.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/serverBootstrap.test.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/test/serverBootstrap.test.ts contains `dependency_analysis`
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `dependency_analysis`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `get_dependency_analysis`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `freshness`
  </acceptance_criteria>
  <done>Phase 7 is discoverable, diagnosable, and freshness-safe from bootstrap through indexed regression coverage.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm run build && tsx --test test/dependencyTools.e2e.test.ts test/multiWorkspaceDependencies.e2e.test.ts test/serverBootstrap.test.ts test/capabilitiesAndHealth.test.ts test/searchIndexIntegration.test.ts` passes
- [ ] `get_dependency_analysis` works over stdio without mutating the workspace fixture
- [ ] Multi-workspace dependency results and explanation-path steps preserve `workspaceRoot` attribution on symbols and evidence
- [ ] Bootstrap, capabilities, and health all advertise `dependency_analysis` and `get_dependency_analysis`
- [ ] Dependency-analysis responses refresh or degrade explicitly instead of leaking stale paths
</verification>

<success_criteria>
- All tasks completed
- Phase 7 is exposed as a real MCP workflow rather than an internal backend only
- Agents can request bounded multi-hop dependency analysis, inspect explanation paths, and trust workspace attribution from the MCP boundary
- Validation covers stdio transport, federated workspaces, metadata discovery, and freshness behavior end to end
</success_criteria>

<output>
After completion, create `.planning/phases/07-dependency-traversal-and-path-explanation/07-03-SUMMARY.md`
</output>
