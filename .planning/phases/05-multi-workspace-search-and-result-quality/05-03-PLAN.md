---
phase: 05-multi-workspace-search-and-result-quality
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - tree-sitter-mcp/src/results/searchRanking.ts
  - tree-sitter-mcp/src/results/workspaceBreakdown.ts
  - tree-sitter-mcp/src/definitions/searchDefinitions.ts
  - tree-sitter-mcp/src/references/searchReferences.ts
  - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
  - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
  - tree-sitter-mcp/src/tools/searchReferencesTool.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/searchIndexIntegration.test.ts
  - tree-sitter-mcp/test/semanticTools.e2e.test.ts
  - tree-sitter-mcp/test/definitionTools.e2e.test.ts
  - tree-sitter-mcp/test/referenceTools.e2e.test.ts
  - tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts
autonomous: true
requirements: [SEARCH-01, SEARCH-02, SEARCH-03]
user_setup: []
must_haves:
  truths:
    - "Broad federated searches still honor workspace, path, language, and symbol-kind narrowing before ranking or truncation happens."
    - "Search payloads explain large multi-workspace result sets with machine-readable per-workspace breakdowns, not just a flat result list."
    - "Exact-match, prefix-match, and contains-match ordering stays stable across multiple workspaces, while single-workspace calls remain concise and backward-compatible."
    - "Phase 5 ships with realistic federated regression coverage that proves attribution, filtering, symbol-kind narrowing, and ranking on duplicate symbols across separate roots."
  artifacts:
    - path: tree-sitter-mcp/src/results/searchRanking.ts
      provides: Shared ranking helpers for symbol and definition searches across multiple workspaces
      exports:
        - scoreNameMatch
        - compareWorkspaceAwareMatches
    - path: tree-sitter-mcp/src/results/workspaceBreakdown.ts
      provides: Per-workspace result-shaping summaries for federated search payloads
      exports:
        - createWorkspaceBreakdown
    - path: tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
      provides: Symbol-search payloads with federated workspace filters, breakdown metadata, and deterministic ranking
      contains: workspaceBreakdown
    - path: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
      provides: Definition-search payloads with federated workspace filters, breakdown metadata, and deterministic ranking
      contains: workspaceBreakdown
    - path: tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts
      provides: Federated search regression coverage across multiple configured roots
      contains: symbolKinds
  key_links:
    - from: tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
      to: tree-sitter-mcp/src/results/searchRanking.ts
      via: symbol search applies workspace/path/language/kind filters across all selected roots, scores the remaining matches once, and only then truncates
      pattern: scoreNameMatch
    - from: tree-sitter-mcp/src/definitions/searchDefinitions.ts
      to: tree-sitter-mcp/src/results/searchRanking.ts
      via: definition search preserves `symbolKinds` narrowing and then reuses the same exact/prefix/contains ranking contract as workspace symbol search
      pattern: compareWorkspaceAwareMatches
    - from: tree-sitter-mcp/src/tools/searchReferencesTool.ts
      to: tree-sitter-mcp/src/results/workspaceBreakdown.ts
      via: reference search reports per-workspace counts so large federated result sets stay explainable
      pattern: createWorkspaceBreakdown
    - from: tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts
      to: tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
      via: the federated E2E suite proves workspace filtering and `symbolKinds` narrowing remain stable when roots share symbol names and paths
      pattern: symbolKinds
---

<objective>
Refine federated search quality and prove the final multi-root search experience on realistic fixtures.

Purpose: Make broad multi-workspace searches understandable at scale by preserving narrowing behavior, improving ranking, exposing per-workspace breakdowns, and validating the final behavior with dedicated federated regression tests.
Output: Shared ranking helpers, additive broad-search tool metadata, and end-to-end proof that Phase 5 works on larger or duplicated repositories without breaking single-workspace callers.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-multi-workspace-search-and-result-quality/05-01-PLAN.md
@.planning/phases/05-multi-workspace-search-and-result-quality/05-02-PLAN.md
@tree-sitter-mcp/src/definitions/searchDefinitions.ts
@tree-sitter-mcp/src/references/searchReferences.ts
@tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts
@tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
@tree-sitter-mcp/src/tools/searchReferencesTool.ts
@tree-sitter-mcp/test/semanticTools.e2e.test.ts
@tree-sitter-mcp/test/definitionTools.e2e.test.ts
@tree-sitter-mcp/test/referenceTools.e2e.test.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add deterministic cross-workspace ranking before truncation</name>
  <files>tree-sitter-mcp/src/results/searchRanking.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts</files>
  <read_first>tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/queries/queryCatalog.ts, .planning/phases/05-multi-workspace-search-and-result-quality/05-02-PLAN.md</read_first>
  <action>Create `tree-sitter-mcp/src/results/searchRanking.ts` with exact exports `scoreNameMatch` and `compareWorkspaceAwareMatches`. Use one ranking contract for federated symbol and definition search: exact case-insensitive name match scores first, prefix matches second, contains matches third, then tie-break by configured workspace order, then `relativePath`, then source offset. Update symbol/definition searches so they collect all candidate matches from the selected roots only after workspace/path/language/symbol-kind filters have narrowed the set, apply this shared ranking, and only then enforce `limit`. Keep reference search's existing `call` before `reference` ordering, but add configured-workspace order before `relativePath` so federated reference output is also stable.</action>
  <verify>Read the ranking helper and then run `cd tree-sitter-mcp && npm test` after Task 3 adds federated assertions for exact/prefix/contains ordering.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/results/searchRanking.ts contains `export function scoreNameMatch`
    - tree-sitter-mcp/src/results/searchRanking.ts contains `export function compareWorkspaceAwareMatches`
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `scoreNameMatch`
    - tree-sitter-mcp/src/definitions/searchDefinitions.ts contains `compareWorkspaceAwareMatches`
    - tree-sitter-mcp/src/definitions/searchDefinitions.ts contains `symbolKinds`
    - tree-sitter-mcp/src/references/searchReferences.ts contains `workspaceRoot`
  </acceptance_criteria>
  <done>Large federated searches no longer depend on discovery order; they use one explicit ranking model before truncation happens.</done>
</task>

<task type="auto">
  <name>Task 2: Wire broad-search tool payloads for federated narrowing and per-workspace breakdowns</name>
  <files>tree-sitter-mcp/src/results/workspaceBreakdown.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts</files>
  <read_first>tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/results/paginateResults.ts</read_first>
  <action>Create `tree-sitter-mcp/src/results/workspaceBreakdown.ts` with exact export `createWorkspaceBreakdown` that returns objects shaped as `{ workspaceRoot, searchedFiles, matchedFiles, returnedResults }`. Add exact additive input/output field `workspaceRoots: z.array(z.string())` to `search_workspace_symbols`, `search_definitions`, and `search_references`, while preserving the existing single-root `workspaceRoot` output as the first configured root for backward compatibility. Keep `symbolKinds` on `search_workspace_symbols` and `search_definitions`, and echo the normalized `filters.symbolKinds` after combined workspace/language/path/kind filtering so federated callers can confirm narrowing was preserved. Add exact additive output field `workspaceBreakdown` to the broad search tools and populate it from the selected workspace set after filtering and truncation. Keep default text concise for one workspace, but when more than one workspace is configured, mention the number of searched workspaces and preserve the structured `workspaceBreakdown` array for callers that need attribution and debugging.</action>
  <verify>Inspect structured tool payloads in updated E2E tests and confirm each broad search tool reports `workspaceBreakdown` with per-root counts.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/results/workspaceBreakdown.ts contains `export function createWorkspaceBreakdown`
    - tree-sitter-mcp/src/results/workspaceBreakdown.ts contains `returnedResults`
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `workspaceRoots: z.array(z.string())`
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `symbolKinds`
    - tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts contains `workspaceRoots: z.array(z.string())`
    - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts contains `symbolKinds`
    - tree-sitter-mcp/src/tools/searchDefinitionsTool.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/src/tools/searchReferencesTool.ts contains `workspaceRoots`
    - tree-sitter-mcp/src/tools/searchReferencesTool.ts contains `workspaceBreakdown`
  </acceptance_criteria>
  <done>Broad federated search tools expose their selected workspaces, preserve kind-based narrowing, and explain where the work happened instead of only shipping one flat list.</done>
</task>

<task type="auto">
  <name>Task 3: Add federated regression coverage and single-root compatibility assertions</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts</files>
  <read_first>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/searchIndexIntegration.test.ts, tree-sitter-mcp/test/semanticTools.e2e.test.ts, tree-sitter-mcp/test/definitionTools.e2e.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/src/tools/searchWorkspaceSymbolsTool.ts, tree-sitter-mcp/src/tools/searchDefinitionsTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts</read_first>
  <action>Add a dedicated federated suite `tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts` that configures two or more temp roots with overlapping `relativePath` and symbol names. Assert these exact scenarios: (1) one `set_workspace({ roots: [...] })` call searches across all configured roots, (2) duplicate symbols return distinct `workspaceRoot` values, (3) `workspaceRoots` filtering removes the other root completely, (4) `symbolKinds` narrowing still works across multiple configured roots for both symbol and definition search, (5) exact-match ranking beats prefix/contains matches inside the narrowed federated result set, (6) `workspaceBreakdown` totals match the returned results, and (7) a legacy one-root `set_workspace({ root })` call still succeeds without requiring `roots` or workspace filters. Extend semantic, definition, and reference E2E coverage to assert the new `workspaceRoots`/`workspaceBreakdown` outputs, including a `resolve_definition` follow-up that pins a duplicate symbol to one `workspaceRoot`, and extend capabilities/health plus index-integration coverage to assert multi-root summaries and stable index reuse per root.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the new federated suite plus updated semantic/definition/reference/health suites all pass.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts contains `symbolKinds`
    - tree-sitter-mcp/test/multiWorkspaceSearch.e2e.test.ts contains `root:`
    - tree-sitter-mcp/test/semanticTools.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/definitionTools.e2e.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/definitionTools.e2e.test.ts contains `symbolKinds`
    - tree-sitter-mcp/test/definitionTools.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/referenceTools.e2e.test.ts contains `workspaceBreakdown`
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `workspaceCount`
    - tree-sitter-mcp/test/searchIndexIntegration.test.ts contains `workspaceRoot`
  </acceptance_criteria>
  <done>Phase 5 finishes with realistic federated regression coverage and explicit proof that legacy one-root usage still behaves correctly.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with the new federated multi-workspace suite and updated search E2E coverage
- [ ] Symbol and definition searches preserve `symbolKinds`/workspace narrowing and rank exact matches ahead of prefix/contains matches across multiple roots before truncation
- [ ] Search payloads expose `workspaceBreakdown` with accurate per-root counts
- [ ] Legacy one-root callers still work without supplying `roots`, `workspaceRoots`, or `workspaceRoot`
</verification>

<success_criteria>
- All tasks completed
- Multi-workspace searches remain understandable even when repositories share names, paths, and symbol kinds
- Ranking and truncation behave deterministically across roots
- Structured payloads explain per-workspace search scope and returned results while preserving symbol-kind narrowing
- Phase 5 is backed by dedicated federated regression coverage rather than only single-root fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-workspace-search-and-result-quality/05-03-SUMMARY.md`
</output>
