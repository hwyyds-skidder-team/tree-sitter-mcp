---
phase: 05-multi-workspace-search-and-result-quality
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - tree-sitter-mcp/src/queries/queryCatalog.ts
  - tree-sitter-mcp/src/queries/definitionQueryCatalog.ts
  - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
  - tree-sitter-mcp/src/definitions/definitionTypes.ts
  - tree-sitter-mcp/src/definitions/definitionFilters.ts
  - tree-sitter-mcp/src/definitions/searchDefinitions.ts
  - tree-sitter-mcp/src/definitions/resolveDefinition.ts
  - tree-sitter-mcp/src/references/referenceTypes.ts
  - tree-sitter-mcp/src/references/referenceFilters.ts
  - tree-sitter-mcp/src/references/searchReferences.ts
  - tree-sitter-mcp/src/tools/listFileSymbolsTool.ts
  - tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
  - tree-sitter-mcp/test/definitionFilters.test.ts
  - tree-sitter-mcp/test/referenceSearch.test.ts
autonomous: true
requirements: [SEARCH-02, SEARCH-03]
user_setup: []
must_haves:
  truths:
    - "Every symbol, definition, and reference match plus follow-up target can carry its exact `workspaceRoot` so duplicate `relativePath` values across roots stay attributable."
    - "Definition and reference narrowing accepts `workspaceRoots` without regressing existing `symbolKinds`, `language`, or `pathPrefix` behavior."
    - "File-symbol lookup and direct definition resolution can disambiguate duplicate relative paths or symbol names via explicit workspace scope instead of silently choosing the first configured root."
    - "Single-workspace callers that omit all new workspace fields keep the same behavior they have today, with only additive inputs."
  artifacts:
    - path: tree-sitter-mcp/src/queries/queryCatalog.ts
      provides: Symbol result schema with workspace attribution
      contains: workspaceRoot
    - path: tree-sitter-mcp/src/definitions/definitionTypes.ts
      provides: Definition match and filter schemas with workspace-aware narrowing
      contains: workspaceRoots
    - path: tree-sitter-mcp/src/references/referenceFilters.ts
      provides: Shared normalization for workspace/language/path filters in reference search
      exports:
        - normalizeReferenceFilters
    - path: tree-sitter-mcp/src/tools/listFileSymbolsTool.ts
      provides: File-symbol lookup that can resolve secondary-workspace files safely
      contains: Requested file is ambiguous across configured workspaces.
    - path: tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
      provides: Direct definition lookup that can pin duplicate symbols to one workspace
      contains: workspaceRoot
  key_links:
    - from: tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
      to: tree-sitter-mcp/src/queries/queryCatalog.ts
      via: indexed symbol records persist the same `workspaceRoot` field that search payloads expose
      pattern: workspaceRoot
    - from: tree-sitter-mcp/src/tools/listFileSymbolsTool.ts
      to: tree-sitter-mcp/src/workspace/resolveWorkspace.ts
      via: file navigation resolves relative or absolute paths across the configured root set instead of only `context.workspace.root`
      pattern: resolveConfiguredWorkspacePath
    - from: tree-sitter-mcp/src/definitions/resolveDefinition.ts
      to: tree-sitter-mcp/src/definitions/definitionFilters.ts
      via: follow-up definition resolution uses `workspaceRoot` and `workspaceRoots` to narrow duplicate symbols safely
      pattern: workspaceRoots
    - from: tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
      to: tree-sitter-mcp/src/definitions/resolveDefinition.ts
      via: the tool must forward exact optional `workspaceRoot` and preserve `kind` narrowing for duplicate names across roots
      pattern: workspaceRoot
    - from: tree-sitter-mcp/src/references/searchReferences.ts
      to: tree-sitter-mcp/src/references/referenceFilters.ts
      via: reference search gains the same workspace/path/language narrowing contract as the broader symbol and definition flows
      pattern: normalizeReferenceFilters
---

<objective>
Make the core search contracts and follow-up navigation workspace-aware.

Purpose: Turn the multi-root foundation into precise backend semantics by carrying workspace attribution through schemas, preserving narrowing logic, and keeping cross-workspace follow-up calls exact before the broader search tools are reshaped in 05-03.
Output: Workspace-aware match/target schemas, filter normalization, precise navigation helpers, and focused backend regression tests for duplicate paths/symbols across roots.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-multi-workspace-search-and-result-quality/05-01-PLAN.md
@tree-sitter-mcp/src/queries/queryCatalog.ts
@tree-sitter-mcp/src/queries/definitionQueryCatalog.ts
@tree-sitter-mcp/src/definitions/definitionTypes.ts
@tree-sitter-mcp/src/definitions/definitionFilters.ts
@tree-sitter-mcp/src/definitions/searchDefinitions.ts
@tree-sitter-mcp/src/definitions/resolveDefinition.ts
@tree-sitter-mcp/src/references/referenceTypes.ts
@tree-sitter-mcp/src/references/referenceFilters.ts
@tree-sitter-mcp/src/references/searchReferences.ts
@tree-sitter-mcp/src/tools/listFileSymbolsTool.ts
@tree-sitter-mcp/src/tools/resolveDefinitionTool.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Carry `workspaceRoot` through every symbol, definition, and reference schema</name>
  <files>tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/queries/definitionQueryCatalog.ts, tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/references/referenceTypes.ts</files>
  <read_first>tree-sitter-mcp/src/queries/queryCatalog.ts, tree-sitter-mcp/src/queries/definitionQueryCatalog.ts, tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/workspace/workspaceState.ts</read_first>
  <action>Add the exact required field `workspaceRoot: z.string().min(1)` to `SymbolMatchSchema`, `DefinitionMatchSchema`, and `ReferenceMatchSchema`. Add exact optional `workspaceRoot` to `DefinitionLookupSchema` and `ReferenceSearchTargetSchema` so follow-up tool calls can pin a target to one workspace. Update `extractSymbols`, `extractDefinitionMatches`, and `collectIndexedFileSemantics` so both on-demand parsing and indexed records populate the same `workspaceRoot` field while keeping `relativePath` relative to the owning workspace root.</action>
  <verify>Read the updated schemas and a sample indexed record, then run `cd tree-sitter-mcp && npm test` after Task 3 adds focused backend coverage.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/queries/queryCatalog.ts contains `workspaceRoot: z.string().min(1)`
    - tree-sitter-mcp/src/definitions/definitionTypes.ts contains `workspaceRoot: z.string().min(1)`
    - tree-sitter-mcp/src/references/referenceTypes.ts contains `workspaceRoot: z.string().min(1)`
    - tree-sitter-mcp/src/references/referenceTypes.ts contains `workspaceRoot: z.string().min(1).optional()`
    - tree-sitter-mcp/src/queries/definitionQueryCatalog.ts contains `workspaceRoot`
    - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts contains `workspaceRoot`
  </acceptance_criteria>
  <done>All search-result primitives now identify their owning workspace explicitly instead of relying on a single global root.</done>
</task>

<task type="auto">
  <name>Task 2: Normalize workspace-aware filters and exact follow-up navigation</name>
  <files>tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/references/referenceFilters.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/tools/listFileSymbolsTool.ts, tree-sitter-mcp/src/tools/resolveDefinitionTool.ts</files>
  <read_first>tree-sitter-mcp/src/definitions/definitionTypes.ts, tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/definitions/searchDefinitions.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/references/searchReferences.ts, tree-sitter-mcp/src/tools/listFileSymbolsTool.ts, tree-sitter-mcp/src/tools/resolveDefinitionTool.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts, tree-sitter-mcp/src/workspace/workspaceState.ts</read_first>
  <action>Extend definition filter schemas with the exact additive field `workspaceRoots: z.array(z.string().min(1)).min(1).optional()` and preserve the existing `symbolKinds` dedupe, lowercase language normalization, and path-prefix normalization when workspace scope is added. Add a new `referenceFilters.ts` module that exports `normalizeReferenceFilters` for `workspaceRoots`, `language`, and `pathPrefix`, matching the same normalization rules already used by definition search. Update `resolveDefinition` plus `resolveDefinitionTool` so descriptor inputs can include exact optional `workspaceRoot`, and only records from that workspace are considered when it is present while existing `kind` narrowing still applies. Update `searchReferences` to accept `workspaceRoots` without weakening current path/language handling. Update `list_file_symbols` so the input schema accepts exact optional `workspaceRoot`, absolute paths can resolve against any configured root, and a relative path that matches more than one configured workspace returns the exact diagnostic text `Requested file is ambiguous across configured workspaces.` instead of silently choosing the first root.</action>
  <verify>Use focused unit tests for filter normalization and backend lookup/search behavior, then run `cd tree-sitter-mcp && npm test` after Task 3 adds coverage.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/definitions/definitionTypes.ts contains `workspaceRoots: z.array(z.string().min(1))`
    - tree-sitter-mcp/src/definitions/definitionFilters.ts contains `workspaceRoots`
    - tree-sitter-mcp/src/definitions/definitionFilters.ts contains `symbolKinds`
    - tree-sitter-mcp/src/references/referenceFilters.ts contains `export function normalizeReferenceFilters`
    - tree-sitter-mcp/src/tools/listFileSymbolsTool.ts contains `workspaceRoot: z.string().min(1).optional()`
    - tree-sitter-mcp/src/tools/listFileSymbolsTool.ts contains `Requested file is ambiguous across configured workspaces.`
    - tree-sitter-mcp/src/definitions/resolveDefinition.ts contains `workspaceRoot`
    - tree-sitter-mcp/src/tools/resolveDefinitionTool.ts contains `workspaceRoot`
    - tree-sitter-mcp/src/references/searchReferences.ts contains `workspaceRoots`
    - tree-sitter-mcp/src/references/searchReferences.ts contains `pathPrefix`
  </acceptance_criteria>
  <done>Cross-workspace follow-up calls become precise and backend narrowing gains explicit workspace/path/language/kind scope instead of implicit single-root assumptions.</done>
</task>

<task type="auto">
  <name>Task 3: Lock backend workspace and kind narrowing with focused regression coverage</name>
  <files>tree-sitter-mcp/test/definitionFilters.test.ts, tree-sitter-mcp/test/referenceSearch.test.ts</files>
  <read_first>tree-sitter-mcp/test/definitionFilters.test.ts, tree-sitter-mcp/test/referenceSearch.test.ts, tree-sitter-mcp/src/definitions/definitionFilters.ts, tree-sitter-mcp/src/definitions/resolveDefinition.ts, tree-sitter-mcp/src/references/searchReferences.ts</read_first>
  <action>Extend `definitionFilters.test.ts` so `workspaceRoots` normalization is asserted alongside the existing deduped `symbolKinds`, normalized `language`, and normalized `pathPrefix` behavior. Extend `referenceSearch.test.ts` to prove `workspaceRoots`, `language`, and `pathPrefix` narrow results without changing the legacy one-root default when those fields are omitted. Leave full cross-workspace `resolve_definition` follow-up validation to the federated E2E coverage in 05-03 so this plan stays focused on backend contracts.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm the definition filter and reference search suites pass with multi-root fixtures.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/test/definitionFilters.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/definitionFilters.test.ts contains `symbolKinds`
    - tree-sitter-mcp/test/referenceSearch.test.ts contains `workspaceRoots`
    - tree-sitter-mcp/test/referenceSearch.test.ts contains `pathPrefix`
  </acceptance_criteria>
  <done>The backend workspace-attribution and narrowing contract is locked before 05-03 broadens tool payload shaping and federated end-to-end coverage.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with focused multi-root definition-filter, resolve, and reference-search coverage
- [ ] Every result schema and follow-up target schema exposes `workspaceRoot`
- [ ] Definition and reference filters preserve `symbolKinds`, `language`, and `pathPrefix` behavior when `workspaceRoots` is added
- [ ] Secondary-workspace navigation never silently resolves a relative path or duplicate symbol against the wrong root
</verification>

<success_criteria>
- All tasks completed
- Core search contracts clearly attribute every result primitive to one workspace root
- Agents can narrow definition/reference backends by workspace, language, kind, and path before result sets become noisy
- Follow-up tools remain precise even when multiple roots contain identical relative paths and symbol names
- Existing single-workspace clients continue to work without sending any new workspace filter fields
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-workspace-search-and-result-quality/05-02-SUMMARY.md`
</output>
