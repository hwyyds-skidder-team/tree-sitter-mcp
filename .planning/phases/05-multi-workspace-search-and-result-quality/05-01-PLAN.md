---
phase: 05-multi-workspace-search-and-result-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/server/serverContext.ts
  - tree-sitter-mcp/src/workspace/workspaceState.ts
  - tree-sitter-mcp/src/workspace/discoverFiles.ts
  - tree-sitter-mcp/src/workspace/resolveWorkspace.ts
  - tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
  - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
  - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
  - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
  - tree-sitter-mcp/src/tools/setWorkspaceTool.ts
  - tree-sitter-mcp/test/workspaceDiscovery.test.ts
  - tree-sitter-mcp/test/semanticIndexCoordinator.test.ts
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
autonomous: true
requirements: [SEARCH-01]
user_setup: []
must_haves:
  truths:
    - "`set_workspace` accepts the current single-root `root` input and an additive multi-root `roots` input without breaking existing single-workspace callers."
    - "Workspace state keeps the legacy aggregate view (`root`, `searchableFiles`, `unsupportedFiles`) while adding ordered `roots` and per-workspace summaries that preserve which files belong to which root."
    - "Persistent index bootstrap, reuse, and refresh happen per real workspace root so duplicate relative paths like `src/app.ts` in two repositories never overwrite each other."
    - "All configured roots share one deterministic exclusion policy, and the configured root order becomes the stable workspace order used by later search plans."
  artifacts:
    - path: tree-sitter-mcp/src/workspace/workspaceState.ts
      provides: Multi-workspace aggregate state and per-workspace summary schemas
      contains: WorkspaceEntrySummarySchema
    - path: tree-sitter-mcp/src/workspace/resolveWorkspace.ts
      provides: Root-list normalization plus path resolution against any configured workspace
      exports:
        - resolveWorkspaceRoots
        - findContainingWorkspaceRoot
        - resolveConfiguredWorkspacePath
    - path: tree-sitter-mcp/src/workspace/discoverFiles.ts
      provides: Discovery over one or more configured roots with workspace attribution on each file record
      exports:
        - discoverConfiguredWorkspaces
    - path: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      provides: Federated readiness and refresh coordination across multiple workspace fingerprints
      contains: replaceWorkspaces
    - path: tree-sitter-mcp/src/tools/setWorkspaceTool.ts
      provides: Backward-compatible workspace setup for `root` or `roots`
      contains: roots
    - path: tree-sitter-mcp/test/workspaceDiscovery.test.ts
      provides: Regression coverage for duplicate relative paths across multiple roots
      contains: workspaceRoot
  key_links:
    - from: tree-sitter-mcp/src/tools/setWorkspaceTool.ts
      to: tree-sitter-mcp/src/workspace/resolveWorkspace.ts
      via: `set_workspace` normalizes `root`/`roots` into one ordered configured-root list before discovery runs
      pattern: resolveWorkspaceRoots
    - from: tree-sitter-mcp/src/workspace/discoverFiles.ts
      to: tree-sitter-mcp/src/workspace/workspaceState.ts
      via: discovery outputs populate aggregate and per-workspace snapshots with explicit `workspaceRoot` ownership
      pattern: workspaceRoot
    - from: tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
      to: tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
      via: the coordinator waits for each configured workspace fingerprint to load or build before `set_workspace` returns ready
      pattern: replaceWorkspaces
    - from: tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
      to: tree-sitter-mcp/src/workspace/discoverFiles.ts
      via: refresh must rediscover every configured root and compare records using workspace-aware keys instead of `relativePath` alone
      pattern: discoverConfiguredWorkspaces
---

<objective>
Create the multi-workspace foundation for Phase 5.

Purpose: Let the server configure more than one workspace root at a time without losing single-workspace compatibility, per-root index freshness, or deterministic file ownership.
Output: Multi-root workspace state, root-resolution helpers, federated discovery/index coordination, and bootstrap tests that lock the new contract before search payloads change.
</objective>

<execution_context>
@/home/hwyyds/.codex/get-shit-done/workflows/execute-plan.md
@/home/hwyyds/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-02-PLAN.md
@.planning/phases/04-persistent-indexing-and-query-freshness/04-03-PLAN.md
@tree-sitter-mcp/src/workspace/workspaceState.ts
@tree-sitter-mcp/src/workspace/discoverFiles.ts
@tree-sitter-mcp/src/workspace/resolveWorkspace.ts
@tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts
@tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts
@tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts
@tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts
@tree-sitter-mcp/src/tools/setWorkspaceTool.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Define multi-workspace state and root/path normalization</name>
  <files>tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts</files>
  <read_first>tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Extend `WorkspaceState` and `WorkspaceSummarySchema` with exact additive fields `roots`, `workspaceCount`, and `workspaces`, where each `workspaces[]` entry includes `root`, `exclusions`, `searchableFileCount`, `unsupportedFileCount`, `lastUpdatedAt`, and `index`. Keep the existing `root` field as the first configured root (or `null` when nothing is configured) so current single-workspace callers still see the same shape. In `resolveWorkspace.ts`, add exact helpers `resolveWorkspaceRoots`, `findContainingWorkspaceRoot`, and `resolveConfiguredWorkspacePath`. `resolveWorkspaceRoots` must accept the `root`/`roots` contract, normalize absolute paths, de-duplicate while preserving order, and reject non-directories. `resolveConfiguredWorkspacePath` must resolve an absolute or relative file path against the configured root set without allowing path escape, and `findContainingWorkspaceRoot` must identify which configured root owns an absolute path.</action>
  <verify>Read the updated schemas/helpers and then run `cd tree-sitter-mcp && npm test` after Task 3 adds regression coverage.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/workspace/workspaceState.ts contains `roots: z.array(z.string())`
    - tree-sitter-mcp/src/workspace/workspaceState.ts contains `workspaceCount: z.number().int().nonnegative()`
    - tree-sitter-mcp/src/workspace/workspaceState.ts contains `WorkspaceEntrySummarySchema`
    - tree-sitter-mcp/src/workspace/resolveWorkspace.ts contains `export async function resolveWorkspaceRoots`
    - tree-sitter-mcp/src/workspace/resolveWorkspace.ts contains `export function findContainingWorkspaceRoot`
    - tree-sitter-mcp/src/workspace/resolveWorkspace.ts contains `export function resolveConfiguredWorkspacePath`
  </acceptance_criteria>
  <done>The server has one canonical representation for multiple configured roots and safe path resolution across them.</done>
</task>

<task type="auto">
  <name>Task 2: Discover and index each configured workspace without cross-root collisions</name>
  <files>tree-sitter-mcp/src/workspace/discoverFiles.ts, tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts, tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts</files>
  <read_first>tree-sitter-mcp/src/workspace/discoverFiles.ts, tree-sitter-mcp/src/indexing/collectIndexedFileSemantics.ts, tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts, tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/src/indexing/workspaceFingerprint.ts, tree-sitter-mcp/src/workspace/workspaceState.ts</read_first>
  <action>Create `discoverConfiguredWorkspaces` that walks each configured root independently, applies the shared exclusions list to every root, and stamps every searchable/unsupported file record with the exact owning `workspaceRoot`. Update the persisted-index flow so the active in-memory record identity is the exact tuple `{ workspaceRoot, relativePath }` rather than `relativePath` alone. Add a new coordinator entry point `replaceWorkspaces(workspaces)` that accepts the ordered configured-root list, reuses each workspace's existing fingerprinted index independently, and aggregates `ensureReady()` / `getFreshRecords()` results in configured-root order. Keep `replaceWorkspace(workspace)` as a one-root wrapper that forwards to `replaceWorkspaces([workspace])` so existing call sites remain valid while the internal coordinator becomes multi-root aware.</action>
  <verify>Use unit tests plus a two-root temp-workspace fixture to prove duplicate `relativePath` values from different roots remain distinct in discovery and refresh.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/workspace/discoverFiles.ts contains `discoverConfiguredWorkspaces`
    - tree-sitter-mcp/src/workspace/discoverFiles.ts contains `workspaceRoot`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `replaceWorkspaces`
    - tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts contains `replaceWorkspace(workspace`
    - tree-sitter-mcp/src/indexing/refreshWorkspaceIndex.ts contains `workspaceRoot`
    - tree-sitter-mcp/src/indexing/buildWorkspaceIndex.ts contains `workspaceRoot`
  </acceptance_criteria>
  <done>Discovery, indexing, and refresh now preserve per-root ownership and can safely handle duplicate paths across multiple repositories.</done>
</task>

<task type="auto">
  <name>Task 3: Make `set_workspace` bootstrap multiple roots and lock the contract with tests</name>
  <files>tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts, tree-sitter-mcp/test/workspaceDiscovery.test.ts, tree-sitter-mcp/test/semanticIndexCoordinator.test.ts, tree-sitter-mcp/test/capabilitiesAndHealth.test.ts</files>
  <read_first>tree-sitter-mcp/src/server/serverContext.ts, tree-sitter-mcp/src/tools/setWorkspaceTool.ts, tree-sitter-mcp/src/workspace/workspaceState.ts, tree-sitter-mcp/src/workspace/resolveWorkspace.ts, tree-sitter-mcp/src/indexing/semanticIndexCoordinator.ts, tree-sitter-mcp/test/workspaceDiscovery.test.ts, tree-sitter-mcp/test/capabilitiesAndHealth.test.ts</read_first>
  <action>Update `SetWorkspaceInputSchema` to accept exact additive input `roots: z.array(z.string().min(1)).min(1).optional()` while preserving the existing `root: z.string().min(1).optional()`. Treat `root` as shorthand for a one-item `roots` array, reject calls where neither is provided, and de-duplicate normalized roots before discovery. In the returned payload, keep `workspace.root` for the first configured root, and add exact structured fields `workspace.roots`, `workspace.workspaceCount`, and `workspace.workspaces`. `set_workspace` must wait for every configured workspace index to reach ready/fresh state before returning success. Add tests for: legacy one-root setup, two-root setup with duplicate `src/app.ts`, and health/capabilities payloads that expose the ordered `roots` list without breaking current single-root assertions.</action>
  <verify>Run `cd tree-sitter-mcp && npm test` and confirm workspace discovery, semantic-index coordinator, and capabilities/health suites pass with both single-root and multi-root fixtures.</verify>
  <acceptance_criteria>
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `roots: z.array(z.string().min(1)).min(1).optional()`
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `workspaceCount`
    - tree-sitter-mcp/src/tools/setWorkspaceTool.ts contains `workspace.workspaces`
    - tree-sitter-mcp/src/server/serverContext.ts contains `createWorkspaceState`
    - tree-sitter-mcp/test/workspaceDiscovery.test.ts contains `workspaceRoot`
    - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts contains `workspaceCount`
    - tree-sitter-mcp/test/semanticIndexCoordinator.test.ts contains `replaceWorkspaces`
  </acceptance_criteria>
  <done>Phase 5 has a backward-compatible multi-root bootstrap contract with tests that prove root ordering, discovery ownership, and federated readiness.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes with single-root and multi-root workspace bootstrap coverage
- [ ] `set_workspace` accepts `root` and additive `roots` inputs without breaking the legacy single-root path
- [ ] Workspace discovery and index refresh preserve `{ workspaceRoot, relativePath }` ownership for duplicate paths across roots
- [ ] Health/capabilities payloads expose ordered roots and per-workspace summaries in an additive, backward-compatible way
</verification>

<success_criteria>
- All tasks completed
- Multiple workspace roots can be configured in one request
- Aggregate workspace state preserves per-root ownership and deterministic order
- Persistent indexing remains per real root/fingerprint instead of collapsing repositories together
- The single-workspace contract still works for current callers that only know about `root`
</success_criteria>

<output>
After completion, create `.planning/phases/05-multi-workspace-search-and-result-quality/05-01-SUMMARY.md`
</output>
