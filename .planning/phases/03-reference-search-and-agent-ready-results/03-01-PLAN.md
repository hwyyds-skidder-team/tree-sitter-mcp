---
phase: 03-reference-search-and-agent-ready-results
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts
  - tree-sitter-mcp/src/queries/referenceQueryCatalog.ts
  - tree-sitter-mcp/src/references/referenceTypes.ts
  - tree-sitter-mcp/src/references/referencePipeline.ts
  - tree-sitter-mcp/src/references/searchReferences.ts
  - tree-sitter-mcp/test/referencePipeline.test.ts
  - tree-sitter-mcp/test/referenceSearch.test.ts
autonomous: true
requirements: [SEM-02]
user_setup: []
must_haves:
    truths:
      - "Reference search is implemented as an on-demand service that reuses the active workspace snapshot, parses only files being scanned, and never creates a persistent index or background cache."
      - "Language-specific Tree-sitter queries capture reference and call-site nodes separately from definition captures for the builtin JavaScript, TypeScript, TSX, and Python grammars."
      - "A caller can search workspace references from either a discovered definition descriptor or a direct lookup request and receive exact usage ranges plus reference-kind metadata."
      - "Unresolved or unsupported reference targets continue to surface as structured diagnostics instead of silent skips."
    artifacts:
      - path: tree-sitter-mcp/src/queries/referenceQueryCatalog.ts
        provides: Language-specific reference and call-site capture definitions
        exports:
          - captureReferenceNodes
          - listReferenceQueryTypes
      - path: tree-sitter-mcp/src/references/referenceTypes.ts
        provides: Shared schemas and types for reference matches and search requests
        exports:
          - ReferenceMatchSchema
      - path: tree-sitter-mcp/src/references/referencePipeline.ts
        provides: Shared on-demand reference extraction pipeline
        exports:
          - collectFileReferences
      - path: tree-sitter-mcp/src/references/searchReferences.ts
        provides: Workspace reference and call-site search service
        exports:
          - searchReferences
      - path: tree-sitter-mcp/test/referencePipeline.test.ts
        provides: Multi-language reference extraction coverage
        contains: collectFileReferences
      - path: tree-sitter-mcp/test/referenceSearch.test.ts
        provides: Reference lookup coverage from symbol and direct lookup inputs
        contains: searchReferences
    key_links:
      - from: tree-sitter-mcp/src/references/referencePipeline.ts
        to: tree-sitter-mcp/src/parsing/parseWithDiagnostics.ts
        via: reference extraction reuses the Phase 1 on-demand parser wrapper
        pattern: parseWithDiagnostics
      - from: tree-sitter-mcp/src/references/referencePipeline.ts
        to: tree-sitter-mcp/src/queries/referenceQueryCatalog.ts
        via: parsed trees are queried through the language reference capture catalog
        pattern: reference.call
      - from: tree-sitter-mcp/src/references/searchReferences.ts
        to: tree-sitter-mcp/src/definitions/resolveDefinition.ts
        via: direct lookup requests resolve a target definition before workspace-wide reference search runs
        pattern: resolveDefinition
---

<objective>
Implement the on-demand reference and call-site retrieval backend on top of the existing workspace snapshot, parser, and definition services.

Purpose: Add the missing symbol-usage half of the semantic search workflow before result shaping and MCP exposure.
Output: A reusable reference query catalog, workspace reference search service, and backend tests proving Tree-sitter-based usage lookup across builtin languages.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-03-SUMMARY.md
@.planning/phases/01-server-foundation-and-workspace-discovery/01-VERIFICATION.md
@.planning/phases/02-definition-search-core/02-01-SUMMARY.md
@.planning/phases/02-definition-search-core/02-02-SUMMARY.md
@.planning/phases/02-definition-search-core/02-03-SUMMARY.md
@.planning/phases/02-definition-search-core/02-VERIFICATION.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reference and call-site query catalog plus shared schemas</name>
  <files>tree-sitter-mcp/src/diagnostics/diagnosticFactory.ts, tree-sitter-mcp/src/queries/referenceQueryCatalog.ts, tree-sitter-mcp/src/references/referenceTypes.ts</files>
  <action>Introduce a dedicated query catalog for usage-oriented captures across the builtin grammars, along with shared schemas for reference results and search requests. Distinguish plain references from call-site captures, preserve exact usage spans and selection ranges, and add any missing structured diagnostic codes needed for unresolved reference search flows.</action>
  <verify>Each builtin language can return raw reference or call-site captures with exact source offsets and normalized reference-kind metadata from a parsed tree.</verify>
  <done>Reference extraction rules and payload contracts exist as reusable infrastructure rather than tool-specific logic.</done>
</task>

<task type="auto">
  <name>Task 2: Build on-demand workspace reference search services</name>
  <files>tree-sitter-mcp/src/references/referencePipeline.ts, tree-sitter-mcp/src/references/searchReferences.ts</files>
  <action>Implement services that iterate `workspace.searchableFiles`, reuse `parseWithDiagnostics`, extract reference/call-site matches, and search by symbol name across the workspace from either a discovered symbol descriptor or a direct lookup request. Reuse the Phase 2 definition resolver to anchor direct lookups, exclude declaration hits from usage results unless explicitly intended, and keep all work local and in process memory only.</action>
  <verify>The services can search references across the active workspace for a resolved symbol target without creating a persistent index.</verify>
  <done>Phase 3 has a reusable backend for reference and call-site retrieval that later tooling can expose directly.</done>
</task>

<task type="auto">
  <name>Task 3: Add multi-language reference backend tests</name>
  <files>tree-sitter-mcp/test/referencePipeline.test.ts, tree-sitter-mcp/test/referenceSearch.test.ts</files>
  <action>Add temp-workspace tests covering TypeScript, TSX, JavaScript, and Python reference and call-site extraction, resolution from Phase 2 symbol payloads, parse-failure propagation, and snapshot-only on-demand behavior. Prove the backend can find usages without mutating files or depending on prebuilt indexes.</action>
  <verify>`npm test` covers reference extraction, call-site identification, structured diagnostics, and no-index behavior.</verify>
  <done>There is automated proof that the Phase 3 backend can find symbol usages reliably enough for MCP exposure.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes new reference pipeline and search suites
- [ ] Workspace reference search parses files on demand from `workspace.searchableFiles` and does not create a persistent index
- [ ] Builtin languages return reference matches with exact usage ranges and explicit reference-kind metadata
- [ ] Direct lookup reference search reuses definition resolution and returns structured diagnostics when the target cannot be resolved
</verification>

<success_criteria>
- All tasks completed
- Reference retrieval backend exists and builds directly on Phase 1 workspace discovery plus Phase 2 definition resolution
- A caller can search references or call sites for a symbol across the workspace without tool-specific parsing logic
- Each extracted usage match carries exact source ranges that later tools can surface directly
- The Phase 3 foundation remains local, read-only, stdio-first, and on-demand
</success_criteria>

<output>
After completion, create `.planning/phases/03-reference-search-and-agent-ready-results/03-01-SUMMARY.md`
</output>
