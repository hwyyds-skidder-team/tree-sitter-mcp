---
phase: 03-reference-search-and-agent-ready-results
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - tree-sitter-mcp/src/server/toolRegistry.ts
  - tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
  - tree-sitter-mcp/src/tools/getHealthTool.ts
  - tree-sitter-mcp/src/tools/searchReferencesTool.ts
  - tree-sitter-mcp/package.json
  - tree-sitter-mcp/README.md
  - tree-sitter-mcp/test/capabilitiesAndHealth.test.ts
  - tree-sitter-mcp/test/referenceTools.e2e.test.ts
  - tree-sitter-mcp/test/serverBootstrap.test.ts
autonomous: true
requirements: [MCP-03, SEM-02, SEM-05, RES-02]
user_setup: []
must_haves:
    truths:
      - "The server exposes a read-only MCP tool for workspace-wide reference and call-site search on top of the Phase 3 backend, with direct lookup chaining through Phase 2 definition resolution."
      - "Tool outputs include concise text summaries plus machine-readable reference results, context payloads, diagnostics, and pagination metadata suitable for AI-agent chaining."
      - "Capabilities, health, README guidance, and package metadata advertise reference-search support and standalone stdio startup without implying persistent indexing or write behavior."
      - "End-to-end stdio tests cover definition-to-reference chaining, direct lookup reference search, pagination, diagnostics, and verify that no persistent index or write path is introduced."
    artifacts:
      - path: tree-sitter-mcp/src/tools/searchReferencesTool.ts
        provides: Workspace reference search MCP tool
        exports:
          - registerSearchReferencesTool
      - path: tree-sitter-mcp/src/server/toolRegistry.ts
        provides: MCP tool registration for reference search
        contains: search_references
      - path: tree-sitter-mcp/src/tools/getCapabilitiesTool.ts
        provides: Updated capability reporting for reference-search query types and tool discovery
        contains: search_references
      - path: tree-sitter-mcp/package.json
        provides: Standalone package metadata for invoking the built server as a CLI
        contains: '"bin"'
      - path: tree-sitter-mcp/README.md
        provides: Standalone usage and MCP workflow documentation for Phase 3 tools
        contains: search_references
      - path: tree-sitter-mcp/test/referenceTools.e2e.test.ts
        provides: stdio end-to-end coverage for the Phase 3 workflow
        contains: search_references
    key_links:
      - from: tree-sitter-mcp/src/server/toolRegistry.ts
        to: tree-sitter-mcp/src/tools/searchReferencesTool.ts
        via: registry wires the reference search tool into the MCP server
        pattern: search_references
      - from: tree-sitter-mcp/src/tools/searchReferencesTool.ts
        to: tree-sitter-mcp/src/references/searchReferences.ts
        via: the MCP tool delegates matching, context shaping, and pagination to the shared backend service
        pattern: searchReferences
      - from: tree-sitter-mcp/test/referenceTools.e2e.test.ts
        to: tree-sitter-mcp/src/tools/searchDefinitionsTool.ts
        via: end-to-end workflows prove agents can chain definition discovery into reference search over stdio
        pattern: search_definitions
---

<objective>
Expose Phase 3 reference workflows through agent-ready MCP tools and package the server for standalone use.

Purpose: Complete the v1 semantic-search experience at the MCP boundary with structured response shaping, workflow validation, and straightforward standalone startup guidance.
Output: A read-only reference-search MCP tool, updated capability and health reporting, package and README polish, and end-to-end stdio coverage for chained agent workflows.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-definition-search-core/02-03-SUMMARY.md
@.planning/phases/02-definition-search-core/02-VERIFICATION.md
@.planning/phases/03-reference-search-and-agent-ready-results/03-01-PLAN.md
@.planning/phases/03-reference-search-and-agent-ready-results/03-02-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add read-only reference-search MCP tooling</name>
  <files>tree-sitter-mcp/src/server/toolRegistry.ts, tree-sitter-mcp/src/tools/getCapabilitiesTool.ts, tree-sitter-mcp/src/tools/getHealthTool.ts, tree-sitter-mcp/src/tools/searchReferencesTool.ts</files>
  <action>Register `search_references` as a read-only MCP tool with Zod schemas and structured payloads that accept either a discovered symbol descriptor or a direct lookup request, plus pagination and context options. Surface the new tool name and reference-oriented query types in capabilities and health responses while preserving the stdio-first, local, on-demand design.</action>
  <verify>An MCP client can call `search_references` over stdio and receive structured content with concise text, diagnostics, context payloads, and pagination metadata.</verify>
  <done>The complete Phase 3 reference workflow is reachable through the MCP server without exposing backend services directly.</done>
</task>

<task type="auto">
  <name>Task 2: Package the server for standalone reference-search use</name>
  <files>tree-sitter-mcp/package.json, tree-sitter-mcp/README.md</files>
  <action>Polish the package for standalone use by adding any missing CLI metadata such as a `bin` entry, documenting the full Phase 3 tool surface and startup flow in the README, and showing how an MCP client can launch the built server for definition and reference workflows. Keep the documentation explicit that v1 remains local, read-only, and on-demand.</action>
  <verify>The package metadata and README make standalone startup and MCP usage clear without implying unsupported transports or persistent indexing.</verify>
  <done>The built server is packaged and documented as a standalone MCP plugin or CLI entry point.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end stdio coverage for chained agent workflows</name>
  <files>tree-sitter-mcp/test/capabilitiesAndHealth.test.ts, tree-sitter-mcp/test/referenceTools.e2e.test.ts, tree-sitter-mcp/test/serverBootstrap.test.ts</files>
  <action>Launch the built server as an MCP subprocess, set a workspace, chain `search_definitions` into `search_references`, assert context and pagination payloads, exercise direct lookup reference search, and verify actionable diagnostics for unsupported or unresolved cases. Update bootstrap and capability coverage to prove the packaged server exposes the full Phase 3 tool surface.</action>
  <verify>`npm test` covers the Phase 3 workflow end-to-end from stdio startup through definition chaining, reference search, context, pagination, and packaging visibility.</verify>
  <done>There is executable evidence that the standalone server is ready for AI-agent semantic workflows.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes updated bootstrap, capability, and reference-tool end-to-end coverage
- [ ] `search_references` returns concise text plus structured reference results with diagnostics, context, and pagination metadata
- [ ] Capabilities and health advertise reference-search support alongside existing definition tooling
- [ ] `package.json` and `README.md` make standalone server startup and MCP usage explicit
- [ ] The server remains read-only, stdio-first, and on-demand with no persistent index
</verification>

<success_criteria>
- All tasks completed
- A user can search references or call sites for a symbol from an MCP client over stdio
- A user can chain definition discovery into reference search with agent-ready structured payloads
- A user can navigate large reference result sets through explicit pagination metadata and concise context snippets
- The standalone package and README make the finished v1 server straightforward to launch and integrate
</success_criteria>

<output>
After completion, create `.planning/phases/03-reference-search-and-agent-ready-results/03-03-SUMMARY.md`
</output>
