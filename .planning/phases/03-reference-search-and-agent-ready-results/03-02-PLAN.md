---
phase: 03-reference-search-and-agent-ready-results
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tree-sitter-mcp/src/references/referenceTypes.ts
  - tree-sitter-mcp/src/context/contextTypes.ts
  - tree-sitter-mcp/src/context/extractEnclosingContext.ts
  - tree-sitter-mcp/src/context/contextSnippet.ts
  - tree-sitter-mcp/src/results/paginateResults.ts
  - tree-sitter-mcp/src/references/searchReferences.ts
  - tree-sitter-mcp/test/referenceContext.test.ts
  - tree-sitter-mcp/test/pagination.test.ts
autonomous: true
requirements: [SEM-05, RES-02]
user_setup: []
must_haves:
    truths:
      - "Every reference result can include normalized enclosing scope metadata and a short source snippet anchored to the same stable source range coordinates as the underlying match."
      - "Pagination metadata is deterministic and explicit, including offset or cursor continuation data plus a reliable `hasMore` signal for large result sets."
      - "Snippet shaping stays concise and agent-ready by bounding length, collapsing whitespace, and preserving enough local structure to identify the match without dumping whole files."
      - "Context extraction and pagination live in reusable helpers instead of being embedded in MCP tool handlers."
    artifacts:
      - path: tree-sitter-mcp/src/context/contextTypes.ts
        provides: Shared schemas for enclosing scope and snippet metadata
        exports:
          - EnclosingContextSchema
      - path: tree-sitter-mcp/src/context/extractEnclosingContext.ts
        provides: Reusable enclosing-scope extraction from parsed trees
        exports:
          - extractEnclosingContext
      - path: tree-sitter-mcp/src/context/contextSnippet.ts
        provides: Stable short-snippet shaping around semantic matches
        exports:
          - createContextSnippet
      - path: tree-sitter-mcp/src/results/paginateResults.ts
        provides: Shared pagination metadata helpers for search tools
        exports:
          - paginateResults
      - path: tree-sitter-mcp/src/references/searchReferences.ts
        provides: Reference search integration for context payloads and pagination metadata
        contains: hasMore
      - path: tree-sitter-mcp/test/referenceContext.test.ts
        provides: Context extraction and snippet regression coverage
        contains: enclosingScope
      - path: tree-sitter-mcp/test/pagination.test.ts
        provides: Pagination and continuation regression coverage
        contains: nextOffset
    key_links:
      - from: tree-sitter-mcp/src/references/searchReferences.ts
        to: tree-sitter-mcp/src/context/extractEnclosingContext.ts
        via: search results attach reusable enclosing scope metadata to each reference match
        pattern: extractEnclosingContext
      - from: tree-sitter-mcp/src/references/searchReferences.ts
        to: tree-sitter-mcp/src/context/contextSnippet.ts
        via: search results attach short source snippets without duplicating formatting logic
        pattern: createContextSnippet
      - from: tree-sitter-mcp/src/references/searchReferences.ts
        to: tree-sitter-mcp/src/results/paginateResults.ts
        via: reference search uses one shared pagination layer for stable tool-facing result windows
        pattern: paginateResults
---

<objective>
Add context extraction, snippet shaping, and deterministic pagination metadata to the Phase 3 reference backend.

Purpose: Turn raw reference matches into agent-usable search results that can be consumed incrementally and navigated directly.
Output: Reusable context and pagination helpers plus enriched reference-search payloads and regression tests.
</objective>

<execution_context>
@C:/Users/Administrator/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/Administrator/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-definition-search-core/02-02-SUMMARY.md
@.planning/phases/02-definition-search-core/02-03-SUMMARY.md
@.planning/phases/02-definition-search-core/02-VERIFICATION.md
@.planning/phases/03-reference-search-and-agent-ready-results/03-01-PLAN.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add reusable enclosing-context and snippet helpers</name>
  <files>tree-sitter-mcp/src/references/referenceTypes.ts, tree-sitter-mcp/src/context/contextTypes.ts, tree-sitter-mcp/src/context/extractEnclosingContext.ts, tree-sitter-mcp/src/context/contextSnippet.ts</files>
  <action>Create reusable helpers that derive enclosing scope names, node kinds, and short surrounding snippets for semantic matches without coupling the logic to a single tool. Keep the payloads normalized across builtin languages and anchored to the same stable source ranges introduced in Phase 2.</action>
  <verify>Any reference match can be enriched with enclosing scope metadata plus a concise snippet that includes the matched symbol range.</verify>
  <done>Context payloads exist as shared infrastructure that later tools can reuse consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Add deterministic pagination metadata to reference search</name>
  <files>tree-sitter-mcp/src/results/paginateResults.ts, tree-sitter-mcp/src/references/searchReferences.ts</files>
  <action>Add one shared pagination layer for large semantic result sets using explicit continuation metadata such as `limit`, `offset`, `returned`, `hasMore`, and `nextOffset`. Integrate it into reference search alongside the new context payloads so large workspaces can be traversed incrementally without rescanning ambiguous pages from scratch.</action>
  <verify>Reference search can return predictable pages with accurate `hasMore` and continuation data while preserving result ordering and context attachments.</verify>
  <done>Phase 3 result shaping supports both context-rich output and incremental paging.</done>
</task>

<task type="auto">
  <name>Task 3: Add context and pagination regression tests</name>
  <files>tree-sitter-mcp/test/referenceContext.test.ts, tree-sitter-mcp/test/pagination.test.ts</files>
  <action>Add tests for enclosing scope extraction, snippet truncation and whitespace normalization, stable selection-range anchoring inside snippets, and paginated result windows that report the correct continuation metadata when more matches remain.</action>
  <verify>`npm test` covers context-rich reference results and page continuation behavior under mixed-language fixtures.</verify>
  <done>Phase 3 has regression coverage that prevents drift in context shaping or pagination semantics.</done>
</task>
</tasks>

<verification>
- [ ] `cd tree-sitter-mcp && npm test` passes context and pagination suites
- [ ] Reference search results can include enclosing scope metadata plus short source snippets without changing exact source ranges
- [ ] Pagination metadata reports deterministic `limit`, `offset`, `returned`, `hasMore`, and continuation data
- [ ] Context extraction and pagination helpers remain reusable, local, and on-demand
</verification>

<success_criteria>
- All tasks completed
- A user can request enclosing scope plus short source snippets for each semantic reference match
- A user can page through large reference result sets and know whether more results remain
- Context and pagination behavior are centralized and deterministic enough for MCP tool consumption
- Result shaping remains compatible with the local, read-only, no-index design
</success_criteria>

<output>
After completion, create `.planning/phases/03-reference-search-and-agent-ready-results/03-02-SUMMARY.md`
</output>
